# Octorato v8: The Kernel

> v7 made a send without receipts impossible. v8 makes an agent run a process: identified, isolated, replayable, and installable like a service.

## 1. What "kernel" means in v8

ROADMAP.md:5 says Octorato wants to be "the kernel they run on"; ROADMAP.md:17 admits it "is not yet a runtime with its own kernel". RFC #0002 (ROADMAP.md:23) is decided today: Octorato stays a governance layer that compiles to harnesses (docs/architecture/multi-runtime.md:4-14), and the kernel is a **contract enforced by hooks, scripts and git**, implemented first on Claude Code 2.1.x and portable by binding rows. The kernel is four primitives every run passes through: PROCESS (a run has a pid, parent, worktree, quota and exit status), ISOLATION (one writer per tree and per file lane, denied not advised), JOURNAL (an append-only, hash-chained, replayable record per process, refusals included) and PACKAGE (skills carry a signed, semver manifest with a tree hash, a tracked lockfile, and verify at doctor and pre-push time; arms are validated and installed, not signed). It is NOT a runtime binary, NOT a scheduler, NOT a message bus, and NOT a daemon: the process table is a locked JSON file (the `connectome/sessions.json` pattern, session-isolation-hook.py:52-75, octo-dim.py:52-62), liveness is read from journal mtimes, and the only periodic work rides the existing `brain-digest` systemd user timer (install-observability-timer.py:29). Two things this shape cannot do, stated plainly: it cannot kill or suspend a running agent (hooks cannot block `SubagentStart`; a breached quota denies every later tool call, so **processes can be starved, not killed**), and it cannot schedule. Both are M4 and stay out of scope.

## 2. The kernel contract: four primitives

| Primitive | Exists today (file:line) | Missing |
|---|---|---|
| PROCESS | Session registry with id, worktree, branch, heartbeat, lanes (session-isolation-hook.py:152-160; octo-dim.py:283-350); per-session worktree fork (octo-dim.py:638-698); `PreToolUse[Agent]` budget halt per arm (hooks.json:94-100, budget-check.py:122-218); runtime 2.1.261 delivers `SubagentStart {session_id, agent_id, agent_type, cwd, transcript_path}`, puts `agent_id`/`agent_type` on every hook fired inside a subagent, and `SubagentStop` carries `agent_transcript_path` (r__subagent-stop__qa-receipt.py:20-21) | No process record for a subagent, no parent link, no exit status, no per-process quota (budget-check.py:99,:192 is per arm, per month). `SubagentStart` and `PermissionDenied` are absent from the brain's event lists (rules.schema.json:122, brain_doctor.py:678, check-hooks-drift.py:42-43, naming-policy.yaml:17, hooks.schema.json:9) although present in the runtime |
| ISOLATION | Broad staging denied on a shared tree (dimension-awareness-hook.py:446-469), per-session file lanes (:471-478), command-boundary splitter (:214-253, :259-307); arm leaks caught after the fact by `check-generic.py` and `.githooks/pre-push` | Destructive git verbs and Bash file mutation by a non-owner are not denied (registry/reflex-triage.yaml:155-159 names the hole; memory `lesson_session_isolation_worktree.md:20`: three builders, one tree, 14 files wiped by `git checkout --`); the dimension gate keys lanes by `session_id` (dimension-awareness-hook.py:98-105,:402), so two subagents of one session are ONE dimension to it; cross-arm writes are audited, not denied (ROADMAP.md:15) |
| JOURNAL | Trace hook, O_APPEND contract (trace-hook.py:141-146, trace-storage.md:77-93); shared iterator (_brain_obs.py:87-121); `brain-trace grep/top/tail` (brain-trace.py:5-18); receipt ledger per session (receipt_ledger.py:235-248); per-turn delegation ledger (d__posttool__delegation-ledger.py:30-46) | Nothing is keyed by process; hook denies never reach PostToolUse; auto-mode denials reach nothing; no replay; traces are "not an audit log" (trace-storage.md:121) |
| PACKAGE | Manifest schema with semver, license, capabilities, dependencies, `additionalProperties: false` (schemas/skill-manifest.schema.json:8-60); validator with `--selftest` (validate-skill-manifest.py:48-83); GitHub installer, zip and sparse-checkout (skills/skill-installer/scripts/install-skill-from-github.py:80,:128); `templates/arm/`, `templates/skill/` | 233 skills (`find skills -maxdepth 2 -name SKILL.md` at bd5b9aa; 234 directories, one without a `SKILL.md`), 0 `skill.json`; installer targets `$CODEX_HOME` (:45), validates only `SKILL.md` presence (:164-169); no verify, uninstall, signature, tree hash or lock; the doctor's `capability-manifest-fresh` (brain_doctor.py:1222-1254) checks `docs/CAPABILITIES.md` freshness, not packages |

Registry ids, existing categories (rules.schema.json:31): `ARCHITECTURE.kernel-process`, `ARCHITECTURE.kernel-isolation`, `FLOW.kernel-journal`, `FLOW.kernel-quota`, `META.kernel-package`. ARCHITECTURE cannot sit at PRESENCE (rules.schema.json:84-86); META can, so `META.kernel-package` is declared GATE explicitly.

Runtime facts every phase obeys: same-event hooks run in parallel, so no phase assumes hook order; a PreToolUse call is denied when ANY hook denies; `PermissionDenied` is documented for auto-mode denials only; the ptable is written outside the hot path (SessionStart, SubagentStart, SubagentStop, CLI) with one exception, a NEW lane claim on a first write, amortized once per path per process, under the same flock and counted in `octo bench`; every writer goes through `kernel_proc.py` under flock plus `os.replace`.

Existence (who may open a journal): a process comes into being in exactly two places, `register()` from the SessionStart and SubagentStart reflexes, and the hot-path gate when a first tool call beats its own register hook (the call IS the process's first trace, which is why the gate never denies on a missing `start` line). An ENDING is not one of them. `append()` creates the journal it writes to, so `r__subagent-stop__proc-exit.py` checks `kernel_proc.has_trace(pid)` (a journal on disk, or a ptable row) and records nothing for a pid the kernel never saw. The harness fires `SubagentStop` for agent ids that never registered, never ran a tool and never produced a transcript; without that check each one materialised a whole process out of a single line, a journal whose only record was an `exit` at seq 0 with no parent and no tool (52 such files in one afternoon, measured). A journal with no ptable row is still shown by `octo top`, with type `?`, never as `main`: `top` reads the disk as well as the table, so it must not claim a type the kernel never learned. Orphan journals are swept by `prune_files()` on the same `PRUNE_AFTER` window as every other journal, and no sooner: the journal is the audit trail, and a shorter window for the files that record an anomaly would delete the evidence of it.

What a trace is, and what the guard costs. A trace has to be evidence of WORK, so a zero-byte journal and a ptable row whose value is not an object are both refused: the first is reachable only from a crash between the create and the first write, the second because `pid in procs` tests the key alone. The row half of that is UNREACHABLE through today's read and is kept as defence in depth, which is a weaker claim than the one this paragraph used to make: `read_ptable` drops a value that is not an object before `has_trace` sees it, so the lookup returns `None` whichever predicate is written there (QA measured it). It stays because it is the same predicate the read applies, at the point of use, for a caller that ever reaches it with a table that did not come through the seam, and the anchor for it hands in exactly such a table. The phantom stops are identifiable at the source too, measured over 90 of them: they carry no `agent_type` and a non-empty final assistant message, and they arrive on a periodic cadence near 32 s rather than tracking tool calls, which reads as a harness-internal side-call surfacing as `SubagentStop`. The guard has one cost, stated plainly because a check that trades one bug for a worse one is not a fix: if a real `SubagentStop` ever landed BEFORE its own `SubagentStart` for a child that ran zero tools, its exit would be dropped and the row would read live until the 900 s TTL. Measured on this machine that window is not reachable: register leads the child's first tool call by 4.45 s to 12.77 s across every real child, the shortest real child lived 55 s, and the register hook itself runs in 50 to 100 ms. The cost is bounded anyway, because `claim_lane` creates the row it needs, so a row-less toolless child holds no lane; what expires late is a liveness reading, not a lock. `has_trace` is off the hot path (only the stop reflex calls it) and costs 48 us with the journal present. The row-shape half of that guard is no longer `has_trace`'s alone: QA measured that one value which is not an object took down `octo ps`, `octo top`, `octo replay`, two `brain_doctor` checks and BOTH register hooks, the last of them silently, because `prune()` raised inside the ptable lock and the hook still exited 0. So the shape is decided once, at the read: `kernel_proc.sane_table` DROPS a value that is not an object and `read_ptable_detail` hands the dropped pids back, so `octo ps`, `octo top` and the doctor name what went missing (a repair nobody can see is its own failure mode) and every locked writer republishes the table without it. A read never writes. That read reports at TWO levels, because they are not the same event, but they are the same VERDICT and QA cycle 4 is why the difference had to shrink to reporting. A dropped ROW costs n named rows; a FAULT is `processes` itself not being an object and costs every row on the machine, unnamed and uncounted (QA measured the same rows rewritten as an array and the isolation gate, reading an empty table as "nobody owns anything", ALLOWED the second writer onto a held lane and let the write that followed republish a one-row table with the intruder holding it). The old claim that a row-level drop "leaves a table you can still trust" does not survive n being ALL of them, one row at a time: corrupting only the HOLDER's row left `fault` empty, both gates allowed, and the next `register` republished the table without that row, so the lane was gone for good. So both gates deny on a drop, `claim_lane` refuses on one, the doctor FAILs on one, and `_publish` carries it forward like a table-level fault; what stays two-level is the message, which names the pids when it can.

Zero rows is the truth about a machine in exactly one state, and the state is about the MACHINE rather than the file: no journal beside the table reads live, and the journals themselves can be walked. **How** the file reached zero rows does not enter into it. That is a correction, not a restatement: the guard used to key on `FileNotFoundError`, so it covered a DELETED table and nothing else, and three cheaper moves walked past it (writing `{"version":1,"processes":{}}` over the file, corrupting every row's value, corrupting one row's value). All three yielded zero rows from a file that exists, all three read as a fresh install, and the reader contradiction this section cites as its own proof reproduced verbatim: `octo ps` said "the kernel has registered nothing on this machine yet" while `octo top` listed four live processes out of the same directory. Any other way of yielding zero rows is a machine whose table somebody else made unreadable or removed, so it is never read as a fresh install. The gates fail CLOSED on it (a gate that cannot read the table cannot know whether a lane is held, and one writer per tree is fail-closed), the listings print it above the table instead of announcing an empty machine, and the doctor FAILs rather than WARNs.

Three more of the same shape, and one residual. A PARSE that fails is a fault whatever the parser calls the failure: `json.load` on 20 KB of nested `[` raises RecursionError, which is neither `ValueError` nor `OSError`, so it escaped every fault leg and left through the gates' outer `except Exception: sys.exit(0)` as rc=0 with empty stdout, empty stderr, no deny, no journal line and no quarantine, which is strictly quieter than the deletion above; the reader catches it by shape and each gate additionally denies around its one ptable read, while staying fail-open for every other exception, where allowing the call is the right failure. A ptable path that is NOT A REGULAR FILE is a fault before it is opened: `os.path.getsize` succeeds on a fifo and `open` then blocks forever, and both gates were still running at 60 s, which is neither fail-closed nor fail-open but a hung session. A `lanes` that is not a list of paths makes its ROW unreadable: `lanes_of` coerced a string, a null or a list of numbers to `[]`, so the row stayed in every listing looking healthy and silently forfeited the path it held. And a JOURNAL the kernel did not delete is a record somebody removed, not a process that ended: `rm journal/<holder>.jsonl` freed the holder's lane with the table intact, so a colliding row whose journal is missing holds its lane, and `prune` keeps that row and faults the table instead of freeing it (bounded by PRUNE_AFTER, so a fault nobody clears still expires; `recovery()` names the row as the thing to delete, because there the row is the evidence). chmod, truncation and garbage in that journal already blocked; only deletion was permissive.

The residual is that the journals are evidence stored in the directory the attacker is reaching. An unreadable journal directory, or a file where it belongs, is unambiguous and faults. A journal directory that is simply GONE is not, so it faults only when the kernel directory still shows the machine has run hooks (`.ptable.lock`, a quarantine copy, `open-pending.json`, anything a writer left). **Stated plainly: an attacker who removes the whole kernel directory leaves a HOME that is byte for byte a wiped cache, and no reader can tell those apart from inside.** That is a strictly larger move than the one this closes, it is measured and allowed on purpose (denying every fresh install is the broken-laptop failure), and it sits beside the interpreter path `recovery()` already documents. Two smaller ones, in the same spirit: a fault whose cause is a missing journal is cleared by deleting the ROW rather than the `_faulted` key, since clearing the key alone re-faults at the next prune; and two SessionStart hooks racing on a machine whose table has never existed can read each other's brand-new journals as "somebody else is running", which is a false fault bounded by one registration and unchanged by this cycle. `_publish` is the one function that replaces the file, so it copies what it is about to overwrite to `~/.claude/.cache/kernel/ptable.corrupt-<utc>.json` first, with the reason and the raw bytes: the lost rows stay readable, the repairs are countable (the doctor escalates on three in 24 h, frequency rather than presence), and the copy is taken at the WRITE, so a reader that repairs in memory and writes nothing still leaves no file. That closed the other half QA found: `octo ps` prunes on read, prune is a writer, so on the normal steady state of one old dead row the listing silently rewrote the file without the corrupt row and the footer that was meant to name it read `dropped == []`.

How long a fault lasts, and how it ends. A fix that only fails closed until the next write is a fix that lasts minutes: `register` publishes even on a faulted table (a hook exiting 0 with no row on disk is the silent failure the seam exists to stop), and it used to publish `fresh_table()` plus its own row, so the fault went with the write. QA measured the whole sequence with the real gate and the real SessionStart hook: denied while faulted, one register later the rows on disk read `['newsess']`, the fault read `''`, the same intruder was ALLOWED and the lane the other process held was transferred to it. SessionStart fires on startup, resume, clear and compact. So the row is still published and the empty base is not: `sane_table` hands back a table carrying the unreadable value under `_faulted` (reason, timestamp, the count, and a bounded slice of the rows, which in the list-shaped case are intact and each carry their own `pid`), every writer republishes it, and `carried_fault` keeps reporting it. Ownership stays UNKNOWN: nothing reads those carried rows as lanes, `claim_lane` keeps refusing, both gates keep denying and the doctor keeps FAILing, for as many registrations as it takes. The way out is a file operation from a terminal where no hook fires, `rm ~/.claude/.cache/kernel/ptable.json` (the next SessionStart rebuilds it, and every lane held right now is forgotten until each process claims again) or repairing `processes` to an object and deleting the `_faulted` key beside it, which keeps the rows the file still has. Deliberately NOT an `octo` subcommand and not an env hatch: a fault denies every hooked write, so a command that clears it is a command the agent can run to clear its own gate, which is the `--release` reasoning taken one step further. Every deny, both listings and the doctor print that recovery, from one string in `kernel_proc.recovery()` so they cannot drift.

Which unreadable, and which absent. QA cycle 3 measured two holes in the paragraph above and both were in the SHAPE of the guard, not its intent. First, `read_ptable_detail` had two `except OSError` legs and they disagreed: the one on `open` carried the fault, the one on the `getsize` above it returned `fresh_table()`. Which leg an unreadable table lands on is chosen by whoever made it unreadable (a directory at the path fails at `open`, an ELOOP symlink fails at the stat), so through the stat leg the whole loss reproduced verbatim, down to zero quarantine files for the doctor to count. Both legs carry now and the anchor is written with a symlink loop. Second, and larger: the attacker's cheap move is not corruption, it is DELETION, and both reach the file through the same door (`rm`, `mv` and a `>` redirect at the kernel directory are denied by the Bash gate; `python3 -c` and `ln -sf` are not, which `test_named_residuals_are_honestly_uncovered` already pinned). Corruption buys a deny-all the attacker also suffers. Deletion bought a silent allow-everything machine: no fault, no copy, no doctor FAIL, no deny, while `octo ps` printed "the kernel has registered nothing on this machine yet" and `octo top` listed two live processes out of the same directory. So an ABSENT table is a fault when any journal beside it reads live by the definition above, and the one honest empty table is an absent file with nothing live next to it. The carve-out is a single explicit argument: `register` writes its own journal before it takes the ptable lock, so it passes its pid to `read_ptable_detail` and that one journal does not count, or every fresh install would fault on its first SessionStart. The cost is one documented step in the recovery, stated there rather than discovered: after the `rm` the machine stays faulted until the next SessionStart writes a table or those journals go quiet.

What the recovery says, and what it may claim. The text used to describe the file's PRE-register shape while the deny it is attached to only persists into the POST-register one, so by the time a human read it "repair `processes` to an object" was a no-op and the only actionable half left, "delete the `_faulted` key", discarded every row the fault carried; QA followed it literally and reproduced the lane transfer. It now describes the post-register state, names `_faulted.rows` as where the carried rows are and the `ptable.corrupt-*.json` copy as where the original is, and says what deleting the key alone costs. The reason for having no subcommand is stated as what is VERIFIED rather than as a fact: the direct file verbs are denied, and the interpreter path is open and measured. Closing that is a real expansion of the Bash gate's verb detection with its own false-positive risk and it is deliberately its own change.

Fixtures, because the counts have to be able to move. The fail-closed fault branch could be deleted from BOTH gates and both selftests still passed with identical counts (3+2/29 and 16+13/5) while `brain_doctor` reported that both isolation gates prove themselves: no fixture covered the branch, so no count could move, and a count that did not move looks exactly like a count that was checked. There are now six, a pair per gate per cause: an unreadable table that must block, a deleted table that must block, and a genuinely fresh install that must ALLOW. The benign leg is what makes the violations mean anything, since without a table that is empty for an honest reason "denies when it cannot read the table" and "denies" are the same measurement. A fixture carries its own kernel state through `_setup.ptable` and `_setup.journals`, because a faulted table cannot be the shared seed. New counts: 18+14/8 and 5+3/32, and deleting the branch now fails both.

What a repair is allowed to cost. The ledger's bound used to be a count of files, and the size of each one is chosen by whoever wrote the corrupt table: QA measured a 46.1 MB ptable producing a 47.7 MB copy, 245 MB of peak RSS, 1.64 s of it INSIDE the ptable lock, and a 20-file worst case near 954 MB. Three bounds now, all in `kernel_proc`: `MAX_PTABLE_BYTES` (8 MiB) is checked with a stat BEFORE the parse, so a table past it is a fault that is never parsed, which is what takes the big read out of the lock window (every locked writer parses through that read); `MAX_QUARANTINE_BYTES` (1 MiB) caps what one copy preserves, recorded as `truncated` next to the original `bytes` so a partial copy is never mistaken for a complete one; `MAX_QUARANTINE` (20 files) and `MAX_QUARANTINE_TOTAL` (8 MiB) bound the ledger together, newest always kept; and a copy older than `PRUNE_AFTER` goes on the register-path sweep, because bounded by count and bytes alone a copy never expired at all and one corruption held the doctor at WARN forever while nothing told the operator where the files were or that removing them was safe (`recovery()` now says both). The record also names WHO: the kernel `pid` the writer acts for, plus `os_pid` and `argv0`, because the process that overwrote every lane on the machine used to leave `ts`, `reason` and the bytes and nothing identifying itself. One copy per EVENT, not per write: the carrier remembers the copy it already has, so carrying a fault forward does not refill the ledger or trip the doctor's three-in-24 h escalation off a single corruption.


How a fault ENDS, which until cycle 5 was a question this file could not answer. `FAULT_KEY` was written by four places, read by two, and removed by NONE: no `pop`, no `del`, on any path. Measured on the F4 lost-lanes fault: `recovery()` prescribes deleting the ROW, QA deleted the row, and the machine stayed faulted before AND after the next register, so the gate printed an instruction the operator had just executed; the blast radius is machine-wide, because one dropped row denied a write to a tree no row had ever touched. Every bound this file claimed for a fault was a bound on something ELSE, and the second measurement proved it: a faulted row aged past `PRUNE_AFTER` pruned, the lane freed, `_faulted` stayed and the deny stayed. So a fault now says what KIND it is and the reader re-derives the ones that can be re-derived (`fault_resolved`). `latched` is a table whose rows were lost: their count is unknown and nothing on disk can say what they held, so only a human clears it, which keeps the F1 protection exactly as sticky as it was. The other three are conditions and end when the condition does. `lost-lanes` names the rows, and it lifts when they are removed, when their journals come back, or when prune expires them. `journal-evidence` lifts when the directory is usable again. `zero-rows` names the pids whose journals read live when the table lost its rows, and lifts when they exit or go quiet, by which time the lanes it was protecting are moot. `carried_fault` stops reporting a resolved one and `_publish` removes the key from the file, so the two readers stop printing a fault whose condition is gone. `recovery()` takes the kind and prints the branch that WORKS for it, which is the other half of the same finding: one text listing every option is the text that told the operator to delete a key which, for this fault, re-faults on the next prune.

Two SessionStart hooks on a table-less machine used to brick it permanently, and the trigger is a first run with two terminals. `register` writes its journal BEFORE it takes the ptable lock (deliberately: published-then-written let a sibling prune delete a row whose journal did not exist yet), so the first hook to take the lock reads an absent table with the sibling's brand-new journal beside it and faulted, and before the paragraph above nothing could ever clear that. It is also the state the documented `rm ptable.json` recovery puts the machine into, so recovery could loop. The fix uses the kernel's own doctrine, `has_trace`'s "a trace has to be evidence of WORK": `live_journal_pids` now ignores a journal that carries only `start` lines, because a registration in flight holds no lane (`claim_lane` creates the row it needs and runs off a tool call), and two registers serialize on the ptable lock anyway, so the second reads the first's published row instead of losing it. It weakens nothing real: a process that has WORKED carries a `tool` line, and journal content cannot be forged without breaking the hash chain. The residual is named: a lane claimed in the window between `claim_lane` and that process's own first journal line would not count, which is one tool call wide and only on a process's first tool call, against a permanent machine-wide lockout.

Liveness reads the CHAINED RECORD, not only the mtime, because F4 was one syscall deep. `lane_owner` guarded DELETION (`_mtime is None`) and `_own_fresh` was mtime-only, so one `touch` on a live holder's journal freed its lane with nothing deleted, nothing edited and the chain intact: `live_journal_pids` returned `[]`, both gates ALLOWED, while the last record in that file said the process was seconds old. That is F4's own sentence, "a missing liveness record selecting the permissive branch", applied to a BACKDATED record instead of a removed one, and the enumeration it shipped with ("chmod, truncation and garbage already blocked; only deletion was permissive") was missing the fourth member of its own list. `touch` is already in the Bash gate's kernel-state verb list, so the threat model named the move before the reader did. `_quiet_for` is now the one definition of "how long has this been quiet", read by `_own_fresh` (TTL) and by `prune` (PRUNE_AFTER), because prune read the raw mtime too and backdating a week dropped the row outright at the next SessionStart without the lane check ever being consulted. The honest claim: this raises the price of faking death from one metadata syscall to rewriting the last line of a hash-chained file, and it is not unforgeable. The direction it can still be wrong in is the safe one, since an mtime touched FORWARD keeps a lane held and expires at PRUNE_AFTER rather than handing it to a second writer.

The wipe residual was understated, and the guard was keyed on the syscall one level up. `journal_evidence_fault` consulted `_kernel_has_history()` only on the `FileNotFoundError` leg, so it covered the journal directory being GONE and said nothing about it being EMPTY or STALE. Four cheaper states allowed on both gates with `.ptable.lock` present and every history marker intact: `rm ptable.json journal/*.jsonl` (the directory stays), a written-empty table plus the same sweep, and either of those with the journals BACKDATED instead of removed. The last two are closed by the chained record above. The first two are closed here, keyed on the result: an EMPTY journal directory on a machine whose kernel state says hooks have run is a sweep, because `register` writes its own journal before it takes the lock, so the kernel cannot produce that state. The stated objection does not apply, since a real fresh install has no kernel directory at all and `_kernel_has_history()` is False on it. What that costs, said rather than discovered: after a week of idleness `octo ps` can prune the last journals without registering one, and the next read faults until any SessionStart writes a journal. A quiet machine is one whose journals are OLD, not one with none. The one residual that stands is unchanged and is still the larger move: removing the whole kernel directory leaves a HOME that is byte for byte a wiped cache.

A damaged journal directory with an INTACT table is a fault too, and it used to be described wrongly rather than missed. F3's fault only ran on the zero-rows path, so a directory that was gone, replaced by a file, unreadable or swept beside a healthy table produced a LANE deny naming a dead pid as "never journaled" and advising the operator to "wait for that process to exit", which cannot happen: silence is measured out of the file that is missing. It is reachable by any cache sweep over `~/.claude/.cache`. `read_ptable_detail` asks the question on every read now, at the cost of a `stat` and an `access` rather than the old `os.listdir` (the directory's size is chosen by whoever writes it), and the rows are left exactly where they are, since the fault is about the evidence and not about them. `lane_recovery` is the other half: a lane held by a row with no journal beside it does not free on silence, and both gates now say so and name what to restore.

ONE INVARIANT, WRITTEN INTO THE SOURCE, because the class reached twelve instances and cycle 6 found three more of it in one review: **a parse that fails answers UNKNOWN, and UNKNOWN never selects the permissive branch.** Every finding above is that sentence violated somewhere different, so it is stated at the top of `kernel_proc.py` with its three parts (three answers and not two; unknown holds; unknown needs a clock the writer does not own), and every reader in the module and both gates were checked against it before any of the three below were written. C-A: `_record_ts` returned None for an unparseable tail, a non-dict tail and a missing `ts`, and `_quiet_for` then fell back to the RAW MTIME, so *stale mtime plus unreadable record* was answered as DEATH; `{"ts":1}` was worse because it PARSED, a number saying 1970 read as 56 years of silence. Appending one line costs nothing and nothing on that path calls `verify()`, so the C4 claim that faking death now costs "rewriting the last line of a hash-chained file" was false, and so was its parenthetical that the Bash gate denies the verbs that reach it: `printf >>` and `touch` deny, `python3 -c 'open(j,"a").write(...)'`, `os.utime` and `perl -e` produce no decision at all. The tail was then checked against ITS OWN FILE - shape (the core keys are there and `ts` does not predate its own `start_ts`), anchor (`start_ts` equals line 0's, which `append` copies forward unchanged) and direction (`ts` never goes backwards) - with any failure UNKNOWN, which reads LIVE. **Cycle 7 walked through all three of those and the sentence above is kept only as the history of the fix, not as the contract**: see the cycle-7 paragraph below. **TTL and PRUNE_AFTER answer differently here and that is the decision**: liveness holds on UNKNOWN, so expiry needs its own clock or `>> not-json` on any journal is a permanent lockout of that tree; the clock is the ROW's `registered_ts`, in the ptable, behind the ptable lock, so shortening it is a strictly larger primitive than producing the UNKNOWN. C-B: the cycle-5 start-only filter reads a property of a FILE and the file is attacker-writable, so truncating a live holder's journal to its own first line (the start line copied verbatim, genuine and chain-valid) or to zero bytes rebuilt the silent allow-everything machine `_absent_table` exists to stop; the named bound, one tool call wide, was the bound on the benign race and not on this branch. A start-only journal is a registration in flight only while its `start_ts` is YOUNG (`START_ONLY_GRACE`, 120 s against a window that is one ptable-lock acquisition wide), read off line 0 rather than off the mtime, because the mtime is the field one syscall sets and the attacker wants the skip. It is cheaper than chain verification and it holds where verification would not: the surviving line of a truncated journal verifies. M-C: the carrier's own `kind` selected which verification ran, so a genuine `latched` fault flipped on disk to `zero-rows` with `pids: []` re-derived vacuously, the gate allowed and `_publish` popped the key and deleted the evidence. This is `lesson_unsigned_input_must_not_select_the_trusted_check` (PR #282) recurring verbatim, and the fix is downward-only: a claimed kind can be as strong as the carrier's shape supports and never weaker (an unknown kind or a pid-keyed kind with no pids reads as `latched`), no kind resolves while the journals cannot be used as evidence, and the docstring's overstated claim is replaced rather than softened, since a writer who can edit this key can also delete it. A present-but-shapeless `_faulted` (`null`, `0`, `""`, `[]`) is a fault too: `.get()` could not tell it from an absent key. And a fault that CLEARS now leaves a `fault` line in the lifting writer's journal, which is the other half of a ledger that only ever recorded the setting.

Cycle 6 residuals, measured and deliberately not closed. The Bash gate enumerates shell VERBS while the effect is what matters, so an interpreter reaches the kernel directory with no decision emitted: `python3 -c 'open("<journal>","a").write(...)'`, `python3 -c 'import os;os.utime("<journal>",(1,1))'` and `perl -e 'open(F,">>","<journal>")'` all pass where `printf x >> <journal>` and `touch -d '1 hour ago' <journal>` deny. Recognising the EFFECT (an interpreter invoked with a `-c`/`-e` program that names a kernel path) is a new detector class with its own false-positive surface, it is the verb-table expansion `recovery()` already declines, and **the claim that after C-A it could no longer transfer a lane was FALSE and is corrected here rather than softened**: QA cycle 7 measured the transfer through that same interpreter path against the C-A reader, on both gates. What an interpreter can buy today is stated in the cycle-7 paragraph below. C-B's grace is a window, not a proof: truncating the journal of a process that started less than `START_ONLY_GRACE` ago still reads as a registration in flight, and a wholly forged young start-only journal always will; the trade is a bounded window against the permanent machine-wide lockout the filter exists to prevent. `live_journal_pids` counts a journal with no readable `start_ts`, so a zero-byte journal caught in the microsecond between `os.open(O_CREAT)` and the first write can raise a zero-rows fault on a concurrent register, which needs a first-ever register racing another in the same instant. M-D stands as forensics: `journal-evidence` re-derives and the empty-directory condition ERASES ITSELF, because the gate's own `journal_deny` and every hot-path tool call write into the directory being faulted about, so a sweep is loud for one hook call and durable only from the first register that carries it forward. Both closes were built and measured. Refusing to write the deny record buys nothing (`g__pretool__kernel.py` is the kernel's own recorder and recreates a journal on the next call), and latching the empty branch as its own kind DOES survive but preempts F4: deleting the only journal on a machine leaves an empty directory, so the more specific `lost-lanes` fault, which lifts when the journal is restored, became a fault only a human could clear, and seven anchors failed on it. The lane denies survive on the intact rows either way. Three smaller ones: `_publish` can pop a fault whose condition returned between the re-derivation and the `os.replace` (prune re-detects on the next register, and only for `lost-lanes`); `prune_locked` is the one locked writer with no pid, so a lift it performs is not journalled; and a carrier written before `kind` existed reads as `latched`, which is fail-closed and turns an old prune-written `lost-lanes` fault into one a human clears.

Cycle 7, C-A again, and the frame rather than the guard. The C-A reader answered the `ts` of the LAST record in an 8 KiB tail window, checked against the two lines around it. QA appended TWO lines, each copying line 0's `start_ts` with `ts` set equal to it, and called `os.utime` once: shape held (`ts` does not predate its own `start_ts`), anchor held (that `start_ts` IS line 0's), and the pairwise direction check compared two lines that were BOTH the attacker's, since nothing looked past `parts[-2]`. A holder that had worked 0 s ago read as quiet for 1200 s, `is_live` returned False and both gates emitted no decision at all. Neither ingredient does anything alone, which is why the measurement is a five-row matrix and not a three-row one: without the stale mtime `_quiet_for` short-circuits and never reads the record; with one appended line the direction check still had a real line to compare against. **Two obvious patches were built and measured and neither is the fix.** The CHAIN does not close it: `prev` is an unkeyed sha256, recomputing it is one more expression in the same `python3 -c`, and with the chain recomputed `verify()` returns 0 while the lane still transfers - so calling `verify()` here would have killed the journal QA happened to build and not the class. The WINDOW does not close it either: three appended lines of 3900 bytes evict every real record from the 8 KiB tail, so any windowed guard reads only what the attacker put there. What every one of those guards had in common is that each asked *is the last record well-formed*, and each could therefore be satisfied by writing a well-formed last record. **So the reader stopped asking that and started answering the FRESHEST credible age in the WHOLE journal (`_journal_age`).** The kernel's own writers only append; appending can only add a candidate to a minimum, and another candidate can only make the answer NEWER. There is no line that can be APPENDED to make this reader say "older" - to move the answer toward death the bytes that already say otherwise have to go, which is deletion or rewriting, and a deleted record is the primitive `lane_owner` already refuses to read as a dead process (F4). Freshest is taken over AGES and not over timestamps, because `max(ts)` hands the answer to a line dated into the future and `_skew_age` reads far-future as infinitely OLD, which is the permissive end; `min(age)` puts that line at infinity where it loses. The four guards survive and now run on EVERY line rather than on the tail, any failure still UNKNOWN. A second attack fell out of the same analysis and is fixtured with the first: a STAIRCASE of appended lines each stepping back by less than `FUTURE_SKEW` satisfies a pairwise direction check at every step, so under the old reader the answer walked past TTL one legal-looking line at a time. **Costs, both real.** The whole journal is read instead of 8 KiB, on a path reached only when the mtime already says stale, so the live path is still one stat. And a torn line ANYWHERE now answers UNKNOWN where before only a torn TAIL did, which means one damaged record holds that pid's lane until `registered_ts` + PRUNE_AFTER instead of TTL - fail-closed, freeable by the operator (`lane_recovery`), and stated here rather than discovered. **Residual, measured and not softened**: an attacker who REWRITES the journal (one `open(p, "w")` through the same interpreter hole) still chooses the answer, because every byte this reader reads is a byte they wrote. The provable claim is exactly the narrow one - APPENDING cannot move the answer toward death - and the interpreter path in the Bash gate remains the residual `recovery()` documents. QA also measured that hardening `registered_ts` is the wrong target: anyone who can write the ptable frees the lane in one edit already, so that route is strictly worse for an attacker than what they have.

Cycle 7, the numbers, and how they are reproducible. The commit at 27d51b3 claimed "an end-to-end harness runs all fourteen attack shapes through the real gate scripts: 6 of 14 blocked at the parent commit, 14 of 14 now". No harness in this repo enumerates fourteen shapes, and the two numbers are different units: **6** is the count of violation FIXTURES that commit added (3 shapes x 2 gates; 53 violation fixtures at 27d51b3 against 47 at its parent, verified with `git ls-tree`), and **14** is the count of paired kernel-state SHAPES that already existed at the parent. Everything the parent enumerated blocked AT the parent: a clean checkout of 27d51b3^ passes its own selftest at 17 block + 7 allow (write) and 30 block + 18 allow (bash). So "6 of 14 blocked at the parent" is false under every reading and is retracted. The committed harness is `run_isolation_selftest`, invoked as `--selftest` on either gate, and its counts are the metric. `scripts/tests/crossversion_isolation.py` is the before/after: it holds the fixtures, the sandbox builder and both gate scripts at the working tree and swaps ONLY `kernel_proc.py` for another ref's, because an older commit's `build_sandbox` does not know today's `_setup` keys and would silently no-op every attack - which is how an unreproducible cross-version number was produced in the first place. Measured with it: the four cycle-7 violation fixtures ALLOW under 27d51b3's reader and BLOCK under this one, and every other fixture blocks under both. It reaches back exactly one commit; swapping in 27d51b3^'s `kernel_proc.py` raises `AttributeError: START_ONLY_GRACE`, so no parent-to-HEAD number is claimed here, by this harness or by anyone.

Cycle 8, and the cheapest lane transfer in this PR's history: ONE appended line, 15 bytes, no `os.utime`, no timestamp forgery, no chain work. `is_live` reads `has_exit` FIRST on the subagent branch, before `_own_fresh` runs at all, so an ending short-circuits every freshness guard cycle 7 installed, and the reader accepted any parseable object carrying `kind == "exit"` - `{"kind":"exit"}`, with no `seq`, no `ts`, no `start_ts`, no `pid`, no `prev`, ended a live process. Both gates went from `deny` to no decision at all and the intruder took the lane. **It is not a regression**: the same line does the same thing at 27d51b3 and at every commit before it. And it BYPASSES cycle 7's invariant rather than violating it - `_journal_age` correctly answered UNKNOWN for that torn file, and UNKNOWN never got the chance to hold. The lesson is about the claim as much as the code: cycle 7's docstring said "appending cannot move the answer toward death" without naming the function, a reader took it as a property of the liveness ANSWER, and monotone-under-append is a property of a MINIMUM OVER AGES that no reader consulting it inherits. The sentence is now scoped to `_journal_age` explicitly. **Validating the line cannot be the fix and the source says so plainly**: a real exit line and a forged one are byte-identical by construction, because the exit hook writes it with the same `append` a forger calls, so any shape, anchor or direction rule it satisfies a forger satisfies by calling that function. So an ENDING is defined as the two writes a real ending makes - `r__subagent-stop__proc-exit.py` appends the journal line and then marks the ROW `exited` through `update_row`, under the ptable lock - and `has_exit` requires both. That does not add a boundary, it removes a cheaper way around the one already there: QA measured that emptying a row's `lanes` frees the lane in one ptable edit, so the ptable was always the floor, and an append-only forgery was a strictly cheaper bypass of it. `has_exit_line` keeps the journal-only reading for the two callers that need it and cannot have the row: the WRITER's idempotence guard (asking the ending would append a second exit line on every re-fired SubagentStop whose `update_row` found no row) and `live_journal_pids`, whose entire contract is to read the journals WITHOUT the process table. That one at least requires a line this kernel could have written, and skipping is its fail-closed direction, since no exit means live. `exited is True`, not truthiness, on cycle 6's `"_faulted": null` precedent. Two windows, stated: the hook appends before it publishes, so between the two writes liveness reads LIVE for an instant (fail-closed, closes on the same hook call); and a child whose register hook lost its race has no row to mark, so it reads live until its journal goes quiet at TTL. **Two existing tests changed their SETUP, not their assertion**, and that is the finding rather than a concession: `test_a_child_with_an_exit_line_is_not_live` and `test_an_exited_holder_holds_nothing` both built an ending by appending one journal line, which is exactly the attack, so they now make both writes. What stopped being covered is "an exit LINE alone ends a process", deliberately, and it is covered in the opposite direction by `test_c8_a_well_formed_exit_line_alone_is_not_an_ending`. **And the cycle-8 fixtures caught a wrong-reason pass in their own first version, which is worth more than the fixtures.** `_setup.forge_exit` called `kernel_proc.append(pid, ...)`, which resolves the journal through `$HOME` - and `build_sandbox` rebinds HOME for the hook SUBPROCESS, never for its own process. The exit line went into the real kernel directory and never into the sandbox, so the violation fixtures BLOCKED because nothing had been forged and the holder was simply alive. Both selftest counts rose and both were meaningless: the "a count that cannot move looks like a count that was checked" failure that cycle 3 F4 already named, reproduced by the person who wrote that sentence down. Nothing detected it from inside - the suite was green, the counts were up, and the fixture read correct. `crossversion_isolation.py` is what caught it, by showing those same fixtures blocking under a reader that could not possibly have blocked them, which is the whole argument for a before/after that runs the OLD code against the NEW fixtures rather than trusting a rising number. Every `_setup` block writes by path for exactly this reason and `forge_exit_line` does too, the harness now carries all fixture machinery across so a missing helper cannot raise `AttributeError` inside `build_sandbox` and read as "blocked", and the leak is proven closed by comparing the live journal's mtime across a full selftest run. Cycle 7's remaining item closed with it: `prune`'s absent-journal branch read `0.0 if registered is None else registered`, and 0.0 is 1970 on every clock in this file, so a row whose journal is gone AND whose `registered_ts` is unreadable was dropped outright with its lanes, no fault and nothing on any surface - the same collapse the invariant names, one branch over from the UNKNOWN case that already handled it correctly. Such a row is kept now, and FAULTED if it holds lanes.

Cycle 9, and the third instance of the class in a NEW SHAPE: not a reader believing something false, but a reader never allowed to finish being right. `_journal_age` reads the whole journal on the stale path, and the attacker owns both inputs to that - the SIZE (append) and the STALENESS (`os.utime`) that makes the reader run at all, both through the interpreter hole. Every guard passes on every line, because the lines do not need to lie, only to be numerous. `~/.claude/settings.json` gives all four PreToolUse gates `"timeout": 5`, and the deny is written to stdout only after the pass returns, so a killed gate emits nothing - and empty stdout is the same `(none)` that every matrix in this PR records as an ALLOW. Measured end to end on the real Bash gate: 0 MB 0.27 s, 5 MB 0.25 s, 50 MB 1.96 s, 300 MB **8.01 s**, past the budget. QA measured the crossing independently between 200 and 300 MB and confirmed that a gate killed at 5 s hands the harness an empty string. **This also breaks cycle 7's narrow sentence a second time, and more cleanly than `has_exit` did**: `_journal_age` is monotone under append in VALUE and unbounded under append in COST, and the gate's decision is the composition of the value with the time taken to produce it. A slow reader is a permissive reader when somebody else owns the clock. **Three bounds, three different jobs, none of them a clock corroborating another.** `MAX_JOURNAL_SCAN` (8 MiB) is an O(1) `getsize` before the file is opened. `MAX_JOURNAL_LINES` (20000) is the deterministic per-file bound, and bytes alone were the WRONG bound: cost is 30 us per LINE, so a byte cap generous enough for real journals still admits an order of magnitude more parsing when the lines are short. Both are sized from the machine rather than from taste - 476 journals on the reference box run to 275 KB and 704 lines at the largest, 28 KB at p95, so each cap is ~27x the largest real journal, and a test asserts that ratio so a future change that grows journals fails here rather than being discovered as a lane that will not free. Past either cap the answer is UNKNOWN, which HOLDS, so padding buys the fail-closed branch instead of a timeout. **And the per-file caps were not enough, which is the part worth reading twice.** They bound a FILE; the attack answered with more files. With both caps in place, twelve capped stale rows took `prune` 4.79 s against the register hook's own 5 s SessionStart budget. So the three callers that fan out over pids - `prune`, `lane_owner` and `live_journal_pids` - open a `SCAN_BUDGET` first (`reset_scan_budget`), and `_journal_age` refuses to start or to continue a pass once it is spent. The budget is TIME and not a line count deliberately: it is a deadline being protected, a cap sized on a quiet machine is not a cap, and QA measured a pristine selftest swinging 1.34 s to 9.27 s under load. Measured after: 12 rows 1.52 s, 60 rows 1.51 s - flat, because the bound is the budget and not the row count. It is reset per FAN-OUT rather than per process so a test making thousands of calls cannot accumulate its way into spurious UNKNOWNs. **And the mutation run caught the new guard being unguarded**, which is worth recording because it is the same defect one level up: `MAX_JOURNAL_LINES` SURVIVED a green suite, because the boundary test written for `MAX_JOURNAL_SCAN` moves the byte constant and nothing moved the line one, so deleting the line cap changed no assertion. A guard added to fix a finding arrived masked by its own neighbour. It has its own two-sided boundary test now. Two things checked rather than assumed while closing this: `g__pretool__kernel.py` shares the 5 s budget but reads no journals (it calls no liveness function), so it is not exposed; and a killed register hook leaves the previous table intact, since `_write_ptable` replaces atomically, so that direction costs a lost registration and not a lane. **Three more from the same cycle, and the first two are the same defect re-read at the next level up.** (1) The kernel-directory floor in the Bash gate was an ALLOWLIST OF VERBS, and an allowlist of verbs loses to the next verb: `rm`, `mv`, `truncate` and `unlink` denied while `mkfifo`, `ln -sf`, `shred -u`, `install` and `busybox rm` all passed undecided, so `mkfifo $HOME/bf && ln -sf $HOME/bf <kernel>/journal/<holder>.jsonl` was allowed and from then on every tree-owner gate blocked inside `open()` (measured >20 s on both), was killed at `timeout: 5`, wrote no stdout and emitted no decision, which reads as ALLOW. No `os.utime`, no 300 MB file: `is_live` -> `has_exit` -> `has_exit_line` opens the journal before any freshness check, so a FRESH mtime wedges it too. The `S_ISREG` guard that closes this had existed since cycle 4 F5 and had been applied to the PROCESS TABLE and to nothing else, which is the cross-function symmetry miss rather than a new class. Both halves are fixed: every open of a journal, a lock and the table now goes through `_kernel_fd` (`O_NONBLOCK` so the open cannot wait, then `fstat` so what was actually opened is what is judged, not what was there a moment ago), and the gate floor is inverted to a NAMED-PATH test - any segment naming a path inside the kernel directory is denied whatever verb carries it unless the program is on a short read-only list, with the raw segment text tested for the interpreter hosts, which closes the `python3 -c`/`perl -e` residual `recovery()` had documented since cycle 3. **The over-fire is real and is the price**: a READ of kernel state through a tool that is not on that list is denied too (`xxd`, a read-only `python3 -c`), and the operator's terminal is not hooked. The mutation run found the two new guards masking each other, the same way `MAX_JOURNAL_LINES` was masked: `O_NONBLOCK` answers a FIFO on its own, so deleting `S_ISREG` left every fifo test green; a CHARACTER DEVICE separates them (`ln -sf /dev/zero <journal>` opens fine and then yields infinite newline-free bytes, measured as a hard hang at 20 s), and each guard has that shape as its own test now. (2) `SCAN_BUDGET` bounds a FAN-OUT and the harness timeout bounds an INVOCATION, which are not the same number. `lane_owner` resets the budget on entry and the Bash gate calls it ONCE PER TARGET, so N targets opened N budgets (instrumented, load-independent: 1/2/4/8/16 -> 1/2/4/8/16), and 8 targets over one stale row with an 8000-line journal measured **6.65 s**, past the kill, with no decision. That is verbatim what this cycle's own commit fixed one level down ("they bound a FILE; the attack answered with more files"); it bounded a fan-out and the attack answered with more fan-outs. The window was NON-MONOTONIC in N, because a fan-out that exhausts its budget denies and short-circuits, so the dangerous sizes were the ones just under the cap. `INVOCATION_BUDGET` (3.5 s, measured from this module's import; process spawn to that import is 0.09-0.21 s over 7 idle runs, against the 5 s kill) is armed by both gates and folded into `reset_scan_budget` as the EARLIER of the two deadlines, so the two compose rather than replace. Measured after, under load: 8/16/32/64 targets at 3.71/3.70/3.82/3.94 s, all denies, all inside the kill; idle, the same 8-target case answers ALLOW in 3.04 s. **The cost, stated:** past the deadline every remaining row answers UNKNOWN, UNKNOWN reads LIVE, so a genuinely stale holder is DENIED rather than freed on a loaded box. Fail-closed, one `stat` per row, and the operator's `octo ps --release` is the early out. (3) `prune` kept a row whose `registered_ts` is unreadable AND which holds no lanes, forever: five injected clock-less rows at now + 70 days, zero dropped, where the parent commit dropped all five. Cycle 7 was right that dropping such a row WITH LANES frees a lane silently, and one case too wide: a row with no lanes grants nothing, so it expires. The ptable is attacker-writable, so what was being kept was unbounded growth in the file every fan-out walks, which is fuel for (2). **And one claim corrected rather than softened**: `has_exit`'s new ptable half does not raise the floor above the pre-existing ptable floor. Its docstring ended "the ptable is behind a lock, `sane_table`, the fault carrier and the quarantine copy", which reads as four checks a forger must beat; QA measured that `exited: true` on the row plus a well-formed exit line frees the lane with all four reading CLEAN. The lock serializes writers, it does not authenticate them, and the fault machinery asks whether the table is readable, never whether a row is true. What that change deleted was the 15-byte append that got the same result with no ptable write at all.

Cycle 10, and the class one level up from cycle 9's floor: the floor stopped being a verb list, and the PARSER that feeds it still was. Cycle 9 inverted the kernel-directory test so any segment naming a path inside it is denied whatever verb carries it. What that left untouched is how a path becomes a path, and four shapes never got there. `install` writes its destination exactly as `cp` does and was in no mutator table, so `install /dev/null <lane>` passed while `cp /dev/null <lane>` denied. `busybox` (and `toybox`) is a MULTI-CALL BINARY, which is a wrapper and not a program, the same shape as `env`, `nohup`, `command`, `exec`, `time`, `nice`, `timeout`, `stdbuf` and `xargs`, all of which were already peeled; only those two were missing. A non-symbolic `ln` makes a SECOND NAME for the same inode, and `realpath` does not resolve hardlinks, so no later test can see the alias and the deny has to be on the act of aliasing itself; `ln -s` is the opposite case and keeps returning only its destination, because a symlink stores a PATH and a write through it resolves back to the lane where the ordinary test already runs. And `$HOME`/`${HOME}` were never expanded although `~` was (`os.path.expanduser` in `resolve`, measured denying at the parent), which is the sharpest of the four because **it also walked around cycle 9's named-path kernel floor**: `rm -f "$HOME/.claude/.cache/kernel/ptable.json"` was allowed at the parent commit while the same path spelled literally denied. The floor inverted the verb and left the path a literal comparison, so one token rewrote the whole test. Expansion is legitimate here for exactly one variable and the source says why: this process HOLDS the value of `$HOME`, so resolving it is reading its own state rather than guessing at a shell it never runs. It is leading-token only and requires a separator or end-of-token after the name, so `$HOMEBREW` and `$HOME_DIR` are left alone. `cp`, `install` and `ln` now share ONE destination rule (`_copy_targets`) instead of three drifting copies: last positional, or the `-t DIR` value, or every positional under `install -d`. The valued-option table deliberately omits `--backup`, `--reflink` and `--sparse`, whose argument is optional and `=`-only, because listing them would consume the first SOURCE of `cp --backup src dst` and turn a covered command into an uncovered one. Measured: nine flips, zero mismatches, against a fifteen-case over-fire set that allows at both commits (`install -m 755` into a build directory, `install -d`, `install -t`, `ln -s` inside a worktree, a symlink OF a lane, a hardlink between unowned paths, `busybox ls`, `$HOMEBREW`, a non-HOME variable, and reading kernel state). 403 tests, bash selftest 43 block + 31 allow, write selftest 24 block + 14 allow. **Residuals, each with the reason it is irreducible here.** A symlink whose source is a lane stays allowed, because denying it would deny the `ln -s` this repo uses daily and a write through it is caught anyway. A hardlink made outside this parser (`rsync --link-dest`, `pax -l`, `python3 -c "os.link(...)"`) is as invisible as any other evaluator, which is the interpreter residual already documented and not a new one. An alias that ALREADY EXISTED when the session started was never watched being made, and since `realpath` cannot resolve it there is nothing to test later. Variable expansion other than `HOME` stays unexpanded for the reason above inverted: the value lives in a shell this gate never runs, and inventing one would deny work nobody owns. **One finding that was not a product of this change**: `test_the_invocation_budget_bounds_the_whole_call_not_one_fan_out` fails under load at BOTH commits, 3 of 3 each, with parse cost unchanged at 3.6-4.2 ms per scan. Its own docstring warns against pinning a verdict the clock decides and then asserts a specific holder's name one line later, which assumes the scan reaches the live lane before the journal budget is spent. It is repinned to what the mechanism guarantees under both outcomes: a holder is named, and when it is not the live one the deny carries the budget sentence.

Cycle 11, and it is not a new class but a CHANNEL neither cycle before it ever looked at, so a floor twice reported closed was open through the way anyone on this machine actually writes a program. Say it plainly, because the paragraph above says otherwise: cycle 10 describes the kernel floor as closed, and it was closed for `-c`. Both verifications used the vocabulary the gate itself names, never the vocabulary the machine uses. **Measured over the harness transcripts rather than reasoned: of 16350 distinct real Bash commands, 3186 carry a HEREDOC and 3601 carry a `-c`.** Both gates covered `-c`; neither covered the heredoc. Reproduced against the Bash gate at dd31481 with a passing positive control: `python3 -c "open('<kdir>/journal/x.jsonl','a').write('x')"` DENY, the identical body behind `python3 - <<'EOF'` ALLOW, behind `python3 <<'EOF'` ALLOW, `python3.12 -c` with the same body ALLOW, and `cat > <kdir>/journal/x.jsonl <<'EOF'` DENY - that last one on the REDIRECT rule, not on the interpreter half, which is exactly what hid the hole from a matrix that only looked at verdicts. With `python3 - <<EOF` the program arrives on STDIN and there is no flag whose argument is the program, so `code_names_kernel_state` was handed an opening line that names nothing while the body was split into "segments" that tokenize to noise. **The fix is in the shared parser, not in either gate**: `split_heredocs` takes the bodies out before anything else is parsed, and `stdin_is_program` hands each body back only to a command that runs stdin AS ITS PROGRAM - no operand, or `-` / `/dev/stdin`, and no `-c` - decided at the LINE level so `cat <<'EOF' | python3 -`, where the declarer and the receiver are different commands, is the same channel. Here-strings and process substitutions are lifted from the merge gate's `_stdin_channel_texts` rather than written a third time, because three drifting copies of a destination rule is what cycle 10 had just finished consolidating. **The same reading closes an over-fire that was already live in the other direction**: a body nobody executes is DATA, and until now every body was split into segments and read as commands, so a `git commit -F - <<'MSG'` whose message quoted `rm -rf` was a deletion. A body is claimed by the SUB-COMMAND that opens it rather than by the command as a whole, which is the difference between a first draft that lost `cd pkg && bash <<EOF / rm -f a.py / EOF` (denied at HEAD, allowed by the draft) and one that keeps it; that regression was caught by a residual test asserting ALLOW and going red, which is the only reason it is not in this diff. **Second half of the cycle, `python3.12`**: `_interp_base` strips a trailing VERSION, because `rstrip("0123456789")` leaves `python3.` with the dot and matches nothing, so the only versioned interpreter on this host (verified: `/usr/bin/python3.12` is the single versioned binary among the interpreters) escaped both the raw-text test and the `-c` re-scan while bare `python3` was caught. Adversarial QA found the identical defect independently in the merge gate's own host match, which makes it one bug in shared logic rather than two. The strip widens the INTERPRETER tables and nothing else: `_KSTATE_READONLY` keeps its exact match, since `base64` normalizes to `base` and a read-only program must not lose its place in that list to a version strip. **The over-fire was measured on the same 16350 real commands, not assumed.** Kernel-floor denies go 200 -> 225: 40 commands newly denied, 15 no longer denied. Of the 40, **20 genuinely reach the live kernel directory through an interpreter** (reads included - the floor has denied reads since cycle 9 and this changes nothing about that), **8 name a SANDBOX directory spelled `.claude/.cache/kernel`** under a test root, which is the relative spelling `_KDIR_SPELLINGS` has carried since cycle 9 and is inherited here rather than introduced, and **12 name the path only in PROSE inside a patch script**, which is this rule's own false-positive class and the same one a `-c` body has had all along. Against them the 15 that stop being denied are all data heredocs: commit messages, fixture writers, patch scripts. Net over-fire +5 in 3186 heredocs (0.16%), and on the lane side the candidate count falls, 7 commands gaining one and 10 losing one. The comparison that matters is that the sibling gate's equivalent change measured 3 false denies in 893 heredocs; this one is the same order and it removes more than it adds. **Residuals, each with the reason it is irreducible here.** A heredoc whose receiver carries a valued option this parser does not know (`python3 -W ignore <<EOF`) puts a non-flag token where an operand would be, so the body reads as data and the channel is missed - it fails toward ALLOW, which is the right failure for a rule whose cost is denying the dominant idiom. A heredoc fed to `ssh` or a container `exec` runs on ANOTHER machine, where this machine's kernel directory is not the one being named. And the 12 prose denies are not reducible without parsing the body's language: the distinction between a path passed to `open()` and a path quoted inside a docstring is a Python question, not a shell one, and a gate that guesses at it would be back to a verb list wearing a different hat. The parse got CHEAPER, which is the honest side effect of not tokenizing documents: 3.02 -> 2.57 ms per scan over a 300-command sample, and the largest real command in the corpus (32359 chars) 32.8 -> 3.0 ms, against a 3.5 s invocation budget. 414 tests, bash selftest 47 block + 34 allow, write selftest 24 block + 14 allow.


Cycles 12 and 13, and the class moved up a level: from "you closed a member and called the class done" to "you ENUMERATED the class and the enumeration was wrong". Cycle 12 answered the previous finding by writing each class out as a list in the source — every construct that runs a command inside a word, every spelling of a heredoc delimiter, every word position a substitution can occupy — and closed five channels with it: a command substitution is a command wherever it sits (`$(…)`, backticks, nested, assignment RHS, redirect target, inside double quotes, inside an unquoted heredoc body); an UNQUOTED terminator makes a body a command channel whoever receives it; the terminator is matched the way bash matches it; a grouping opener is not the receiver; a project runner is a wrapper BY SHAPE; and the parse is BOUNDED, because a harness kill produces no verdict and no verdict reads as ALLOW. Cycle 13 then measured the list itself and found two rows false. The row saying `$((expr))` is "ARITHMETIC, not a command, left verbatim on purpose" is a false statement about the shell: bash expands `$(cmd)` and backticks inside `$(( ))`, `(( ))`, `$[ ]` and `let` before evaluating anything, measured on bash 5.2.21 with four shapes that each remove a file. **The convergence test with the merge gate ran green over that divergence**, because its corpus held only `echo $((1 << 3))` — an arithmetic expression with nothing inside it to disagree about — which is the same shape as a fixture set that avoids the bug; the corpus now carries four cases measured RED against the provider at e0f9444 before the fix. The second false row is the WRAPPER table, and the reason it looked right is the measurement: it was taken against the KERNEL DIRECTORY, where the named-path floor denies whatever verb arrives, so a wrapper that was never peeled still produced a deny. Re-measured against a REAL HELD LANE with `rm -f <lane>` as a passing positive control, eight wrappers installed on this host walked — `setsid -w`, `flock`, `ionice`, `taskset`, `unshare`, `strace`, `systemd-run`, `watch -x` — and `setsid -w` is mandated by the hard rules of every brief this repo is built under, so it stood in front of nearly every command an agent here runs. So the wrapper stops being a table: a head this file models NOTHING about is peeled forward to the first token it does model, bounded by remote runners (a container or an object store is another file system), script runners (`npm run rm` names a package.json script), the `install` collision (every package manager's subcommand), a reserved word and a control token (the scan depth it also carried was deleted in cycle 14, see below). With it: a `-c` bundle is `-c` (cycle 14 corrected the rule to CONTAINS `c` on a modeled shell, because `bash -ce` deletes the file and the cycle-13 sentence saying otherwise was false about bash); `eval` re-parses its joined arguments; a heredoc is paired with the operand that reads it BY DESCRIPTOR (`python3 /dev/fd/3 3<<'EOF'`) and the `//dev/stdin` path forms collapse; a MISSING terminator is still a heredoc, because bash warns and runs the body anyway; an opener inside a substitution keeps its host (`x=$(python3 - <<EOF …)`); a computed write target reaches a LANE and not only the kernel directory; and the character budget is charged where the characters are WALKED — the opener loop, each parse frame, an unquoted body — instead of tested after the walking, which is what left multi-megabyte shapes at 13.3 s and, under a 5 s emulated kill, rc 124 with zero bytes on stdout. The cap is re-measured under real load rather than at idle: 256 KiB let a 200 KiB single-line command cost 6.1 s at load 18, past the harness timeout; 96 KiB looked safe there and was not, because the shape measured was the wrong one; see cycle 14. **The wrapper class is not closed and the source says so.** What changed is that closing it no longer depends on naming members: the shape covers them. The shape has a stated end — the stop words and the four bounds — and cycle 13's residuals are pinned by test: a wrapper whose program is COMPUTED and a python heredoc body that deletes a lane. The third one it claimed, a wrapper hiding its program past the scan depth, was retired in cycle 14 when the depth itself was deleted. Cycle 13's over-fire numbers did not reproduce and are restated in the cycle 14 paragraph.
Provenance of the cycle labels in this PR's code. The in-code labels ran `QA cycle 3..6` while HEAD's own commit message called the last set `QA cycle 4`, so grepping either number found half the record. They are renumbered to the commit message's line, per PR: cycle 1 = the review that produced `f5a480b`, 2 = `bb4d374`, 3 = `6c760e7`, 4 = `2672773` (F1, F1c, F2, F3, F4, F5, F6), 5 = the review that produced C1-C4 and M1-M2 above. Labels from the merged PR #294 also read `QA cycle 1` in the same test files, and its `QaCycle1`/`QaCycle2` classes are already there at this branch's base; the in-code labels here started at 3 to avoid colliding with those class NAMES, not because the reviews were the third and fourth of a shared sequence, which is what the commit message says and what this renumbering follows. #294's findings are D-numbered and this PR's are F- and C/M-numbered, which is what tells the two apart at a glance.

Cycle 14, and the pattern moved once more: from the enumeration to the ALLOW-LISTS THAT STAND IN FRONT OF THE MODEL. Three of them, and each had the same shape. **The read-only list could write.** `_KSTATE_READONLY` existed so that reading kernel state stays possible, and the header called it "the set of programs that provably cannot create, replace or unlink a path". `sort -o /tmp/f /etc/hostname` and `uniq /etc/hostname /tmp/f` each wrote 11 bytes, and `less --log-file` writes its log; all three were on the list, all three allowed at the gate, while `curl -so` and `gzip` on the same target denied. The floor worked for every program NOT on the list, so the list was the hole, for the third time on this stack. It means something narrower now, and something that can be checked: these are programs whose POSITIONALS are inputs. A path handed to a FLAG is a write on every program including this one, `sort`/`uniq`/`less`/`more` are off the list, and a test re-runs the `--help` screen that found them — while saying out loud that the screen reads FLAGS and `uniq`'s write is a POSITIONAL, which is the member it missed and a human found by running it. **The trigger list was the last hand-written verb allow-list in the file**, and it had drifted from the tables the scan dispatches on, so `curl -so <lane>` and `gzip <lane>` were modelled correctly by their own helpers and `scan` returned `[]` before calling either. It is DERIVED from those tables now, so adding a program arms its trigger in the same edit, and a test asserts the derivation. **And the scan depth was a chosen number that real traffic exceeded**: `_PEEL_SCAN = 8` carried the sentence "measured, not chosen", and `env -u A -u B -u C -u D <program>` chains put the program past it, so `setsid -w env -u GIT_DIR … rm -f <lane>` walked — the shape the hard rules of every brief here mandate. `_WRAPPERS` became a peel target so an option chain is consumed by the table that understands it, and the number was deleted rather than re-chosen. The cap story repeated too, one level down. Cycle 13 measured one long word, which is `shlex`-quadratic and looked like the ceiling; MANY SHORT TOKENS is worse, because every token is resolved and every hit costs an ownership lookup. End to end through the real hook against a real held lane: 95 KiB of `rm -f x x x …` cost **6.63 s sequential and 9.85 s with three concurrent**, against a `timeout: 5` whose kill writes nothing and reads as ALLOW. So tokens are charged where they are spent, 8192 of them and 48 KiB, both from that same end-to-end measurement (2.22 s sequential and 2.87 s concurrent AT the cap, and a deny in about a second above it), and the largest of 19403 distinct real commands here is 4429 tokens. Above either cap the answer is DENY, and the source now says so at the constant, which is the discipline [[lesson-silence-is-not-an-answer-find-the-default]] asks for. Members past the stated end, each measured deleting under bash and allowing at the gate, and the two that matter by traffic are the ones the header had left out: a **PIPE into a shell** (`echo "rm -f <lane>" | bash`, 61 real commands, while the header listed heredoc, here-string and process substitution as "the same channel in other syntax"), and **output flags** (`curl -o`, `wget -O`, 595 real commands). Then `env -S`, `watch "cmd"`, `bash -c --`, `bash -ce`, `git -c alias.x='!cmd'`, `source <(…)`, `find -ok`, and `gzip`/`tar --remove-files`/`zip -qm`. The lane-side tables are stated AS enumerations with an end, because on a lane there is no floor and only knowing what a flag means separates `curl -o <lane>` from `grep -f <lane>`. **And five of cycle 13's reverted-fix anchors were green**, which is the finding about the method rather than the code: a test that never puts the lane in the position the rule decides cannot fail when the rule is deleted, and two mechanisms covering each other make both look anchored when neither is. They are repaired, and cycle 14's own reverts found the same shape twice more in its own work, which is recorded rather than hidden: the wrapper handoff and the deleted depth cover each other for lane detection, and what the handoff uniquely carries is the CWD (`env -C <dir>`), so that is what its anchor asserts.

Liveness (one definition, used by every gate): main process live = its own journal mtime, or any child's journal mtime, within TTL 900 s (session-isolation-hook.py:48), so a parent waiting on a long child never reads dead; subagent live = no `exit` line AND parent live AND own journal mtime within TTL 900 s, so a killed or hung agent expires after 15 minutes and releases its lanes (a single Bash call is capped by the harness well under that, so an active child never expires). Every whole-tree deny names the holding pid, its type and its age; `octo ps` shows them and `octo ps --release <pid>` (journaled as `release`) lets the operator free a stuck lane from the terminal, where no hook fires; it is agent-proof twice: the CLI refuses inside an agent shell via the existing `_looks_like_agent_shell()` markers (octo-dim.py:492, the `approve-prod` precedent), and `g__pretool-bash__tree-owner.py` denies any command whose boundary-split tokens invoke `octo` or `octo.py` with `--release`, so stripping the markers with `env -u` does not help.

## 3. Phases

Every phase: one PR from its own worktree, fixtures under `registry/fixtures/<rule-id>/`, a `--selftest` whose locator is `scripts/<script> --selftest registry/fixtures/<rule-id>` on every GATE mechanism (bespoke or not; `check_incident_fixture_coverage` takes the fixture dir from the first token after `--selftest`, brain_doctor.py:1508-1513; reflex `r__` scripts need no fixture pair, only `g__` names are checked by `incident-fixture-coverage`, and `check_naming` at brain_doctor.py:1148-1152 checks only the name scheme), one new `CHECKS` row (brain_doctor.py:1700-1730), a registry row, a CLAUDE.md anchor, hooks.json entries, `docs/CAPABILITIES.md` regenerated, a QA receipt from a Reality Checker on the judgment tier before `gh pr merge` (qa-merge-gate.py:501-527), master at `brain_doctor` 0 fail after merge. Selftest runner per phase: 1a, 1a-2, 1b, 2 bespoke (state-dependent seeds, dimension-awareness-hook.py:506 precedent); 3 `gate_selftest.run_gate_selftest` with a `home/` seed (gate_selftest.py:123-126); 4 golden-replay compare; 5 bespoke with a throwaway key. Timing is never a gate: unit tests assert the hot path runs and journals, never its duration.

### Phase 1a: PROCESS + JOURNAL library, register hooks, journal gate, schema, tests

Goal: every main loop and every subagent gets a pid, a parent, a worktree, and an append-only hash-chained journal; no tool call runs unjournaled.

Files (create): `scripts/kernel_proc.py` (library, stdlib only: ptable at `~/.claude/.cache/kernel/ptable.json` under flock + `os.replace`; journal at `~/.claude/.cache/kernel/journal/<pid>.jsonl`; `append(pid, record)` takes `fcntl.flock` on `<pid>.jsonl.lock`, reads the tail line, sets `seq`, `prev = sha256(previous line)` and copies `start_ts` forward on every line so the hot path never seeks to the head, appends with O_APPEND, releases; every line under 4096 bytes, longer payloads truncated with a `trunc` marker; `is_live(pid)` per §2; `prune()` from register paths only); `scripts/r__session__proc-register.py` (SessionStart: pid = session_id, worktree = payload `cwd`; `sessions.json[sid].worktree`, when present, recorded as `dim_worktree`; no ordering assumption, the fork hook only prints an instruction, session-isolation-hook.py:225-230); `scripts/r__subagent-start__proc-register.py` (SubagentStart: pid = `agent_id`, ppid = `session_id`, type = `agent_type`, worktree = enclosing `.git` root of `cwd` (dimension-awareness-hook.py:310-319); transcript path unknown at start); `scripts/g__pretool__kernel.py` (PreToolUse `*`, the ONE hot-path script: pid = payload `agent_id` else `session_id`; appends a `tool` line {tool_name, tool_use_id, sha256(tool_input), cwd}; creates the journal directory itself; denies ONLY on an `OSError` from the append, never on a missing `start` line or ptable entry, so a call that races its own register hook is not denied; the deny reason prints the unlock verbatim: `export OCTO_KERNEL_OPEN=1` in the shell that launched Claude Code, then restart; `OCTO_KERNEL_OPEN` is read from the hook's process env only (the `CLAUDE_SESSION_ID` precedent, dimension-awareness-hook.py:98-105), a payload field never counts; calls made in open mode are written as `kind: open` once the journal is writable again, so the doctor can count how many ran unjournaled; Phase 3 adds quota to this same script, no second invocation); `schemas/kernel-journal.schema.json` (kinds: start, tool, exit, deny, receipt, quota, open, release); `scripts/tests/test_kernel_proc.py` (chain under 4 concurrent writers x 200 lines verifies; unlocked writer breaks it; truncation; liveness rules incl. expired child and waiting parent; open-mode backfill); `registry/fixtures/ARCHITECTURE.kernel-process/{start.json,subagent_start.json,home/}`; `registry/fixtures/FLOW.kernel-journal/{violation.json (journal path is a file, must deny), benign.json, benign_no_start.json (no register line, must allow), home/}`.
Files (modify): hooks.json (SessionStart, SubagentStart, PreToolUse `*`); hooks.schema.json:9 event regex adds `SubagentStart|PermissionDenied`; registry/rules.schema.json:122 enum adds both; scripts/brain_doctor.py:678 `CC_EVENTS`; scripts/check-hooks-drift.py:42-43 `KNOWN_EVENTS` (its schema validation at :88-99 would otherwise fail `hooks-runtime-sync`); registry/naming-policy.yaml:17 scheme adds `subagent-start|pretool|permission-denied` (event tokens only; matchers live in hooks.json and `firing_matcher`, accepted by `Mechanism.verify` at brain_doctor.py:739-745); scripts/merge-hooks-cursor.py (drop SubagentStart/PermissionDenied with a note, multi-runtime.md:64 precedent); .githooks/pre-push (runs `python3 -m unittest discover scripts/tests` in the WIRE-GATE stanza, bench excluded); registry/rules.yaml; CLAUDE.md (anchor "Kernel: process and journal" under Session/Instance Isolation); docs/architecture/trace-storage.md (one paragraph).
Registry: `ARCHITECTURE.kernel-process` REFLEX, `gateable` absent (a `gateable: false` needs `v7_decision`, rules.schema.json:69), two `r__` mechanisms, proofs IN_HOOKS_JSON x2 + EXIT_CODE `scripts/r__subagent-start__proc-register.py --selftest registry/fixtures/ARCHITECTURE.kernel-process`; `FLOW.kernel-journal` GATE, fail-closed, gateable, mechanism `g__pretool__kernel.py` at PreToolUse `*`, EXIT_CODE `scripts/g__pretool__kernel.py --selftest registry/fixtures/FLOW.kernel-journal`.
Selftest (bespoke, sandbox HOME, `CLAUDE_SESSION_ID=__selftest__` as gate_selftest.py:95 sets): feed start, subagent_start, three tool payloads with `agent_id`; assert ptable has parent and child, the child's journal has start + 3 tool lines, the chain verifies, the violation seed denies with the unlock text, both benign seeds allow. Imports on the hot path limited to json, os, fcntl, hashlib, time; no git spawn.
QA proof: launch one subagent from a fresh session; `cat` the ptable (session and agent, parent link, worktree); the agent's journal shows its tool lines with `agent_id`; edit one journal byte, `python3 -c "import kernel_proc; kernel_proc.verify(pid)"` returns non-zero; make the journal dir a file and see the next tool call denied with the unlock text; set `OCTO_KERNEL_OPEN=1` in the launching shell, restart, see calls run and `open` lines appear once the dir is restored.
Size: 8 new files, 11 modified, about 850 lines (library 320, hooks 3 x 90, schema 80, tests 250).

### Phase 1a-2: exit hook and the first doctor check

Files (create): `scripts/r__subagent-stop__proc-exit.py` (SubagentStop: `exit` line with ok|error from `last_assistant_message`, `agent_transcript_path`, tool count, duration; reads `spawnDepth`, `model`, `toolUseId` from `<session-dir>/subagents/agent-<id>.meta.json` when present; ~90 lines, the r__subagent-stop__qa-receipt.py precedent is 88); `registry/fixtures/ARCHITECTURE.kernel-process/subagent_stop.json`.
Files (modify): hooks.json (SubagentStop); rules.yaml (`ARCHITECTURE.kernel-process` gains the third mechanism and proof); scripts/brain_doctor.py:1700 `CHECKS` gains `kernel-process-live`.
Doctor check `kernel-process-live`: both selftests pass; every live pid in the real ptable has a journal; the newest 5 journals verify; `open` lines in the last 7 days are counted and printed; a fallback note prints when the runtime lacks `SubagentStart` (then `PreToolUse[Agent]` + SubagentStop only, the child's start line written at its first tool call).
QA proof: a subagent that finishes normally shows an `exit ok` line; stop a subagent with `TaskStop` and record whether `SubagentStop` fires; if it does not, the TTL is the only release and the doc says so.
Size: 2 new files, 3 modified, about 250 lines.

### Phase 1b: `octo` CLI (ps, top, replay, journal, bench)

Files (create): `scripts/octo.py` (subcommands: `ps` (live and exited, pid, ppid, type, worktree, tools, exit, age; prune on read; `--release <pid>` journals a `release` line and frees the lanes; refuses inside an agent shell per §2); `top` (live and last 24 h by tool calls, denies, tokens when the runtime exposed them); `replay <pid> [--verify]` (start, tool lines in order, children, exit; pairs `tool` and `deny` by `tool_use_id` and prints the call as `REFUSED <rule>`; joins the receipt ledger by session_id, receipt_ledger.py:270-274); `journal <pid>` (raw); `bench` (20 timed hot-path invocations including a first-write lane claim, prints the median, exits 1 above 100 ms when run by hand)); `scripts/tests/test_octo.py` (no timing asserts); `registry/fixtures/ARCHITECTURE.kernel-process/replay/` (golden journal with one refused pair and its expected output; a subdirectory is inert to `run_gate_selftest`, which globs only the top level, gate_selftest.py:114-115).
Files (modify): scripts/install-runners.py:40 `VERBS` gains the `octo` thunk; `ARCHITECTURE.kernel-process` gains proof EXIT_CODE `scripts/octo.py --selftest registry/fixtures/ARCHITECTURE.kernel-process`; `kernel-process-live` extended: golden replay matches byte for byte, bench reported as WARN above 100 ms, never FAIL; docs/wiki/Getting-Started.md.
QA proof: `octo ps` shows the Reality Checker itself as a live child with its parent and age; `octo replay <own pid> --verify` prints its tool calls; `octo bench` prints a median (recorded in the PR body, 81 ms is the comparable for delegation-ledger on the reference box).
Size: 3 new files, 4 modified, about 600 lines.

### Phase 2: ISOLATION, one writer per tree and per lane, enforced

Goal: the weekend failure becomes a violation fixture. A process cannot touch a path the journal attributes to a different live process, parent included, and cannot write into another arm.

Rule (sibling rule): on the first Write/Edit, a process claims the enclosing worktree root and a per-pid file lane for that path in the ptable (kept in the ptable, never in `sessions.json`). A write, a destructive git verb, or a Bash mutation targeting a path whose lane belongs to a DIFFERENT live process is denied (a parent-owned tree does not license a sibling to touch a sibling's file; and a parent RELEASES its own lanes when it spawns a child, journaled as `release` with reason `delegate`, so an edit-then-delegate on the same file is not self-denied, while a parent lane claimed after the spawn still binds); lane matching is equality or path prefix, so `rm -rf <dir>` is denied when any other live lane sits under `<dir>`. A whole-tree verb (`git checkout <branch>`, `git switch`, `git reset --hard|--merge|--keep`, `git stash` except `list|show`, `git clean -f`, `git worktree remove`) is denied whenever any other live process holds a lane in that root, and the deny names that pid, type and age. Cross-arm: a write whose target is under an arm root from `company/config/arms-paths.json` different from the process's own arm root is denied; fail-open when the config is absent (public adopters have no arms). The dimension gate keeps its session-scoped lanes in v8.0; both gates deny independently and either deny wins.

Files (create): `scripts/g__pretool-write__tree-owner.py` (Write|Edit|NotebookEdit|MultiEdit); `scripts/g__pretool-bash__tree-owner.py` (Bash: splits on unquoted boundaries by importing `_split_subcmds` and `_broad_git_verb` from dimension-awareness-hook.py:214,:259 through importlib, the receipt_ledger.py:90-96 pattern; git verbs above plus `rm`, `mv`, `cp` (destination), `sed -i`, `tee`, `>`/`>>` redirect targets, `git checkout -- <pathspec>` and `git restore <pathspec>` resolved against `cd`, `-C`, `--work-tree`; a mention inside quotes never fires); both journal a `deny` line; `registry/fixtures/ARCHITECTURE.kernel-isolation/`: `violation_sibling.json` (parent owns the tree, child A wrote `a.py`, child B runs `git checkout -- a.py`: the weekend), `violation_write.json`, `violation_rm.json` (B runs `rm -rf` on the directory holding A's lane, the prefix case), `violation_reset.json` (`git reset --hard` with two live lanes), `violation_arm.json` (seeded arms-paths), `benign.json` (A checks out its own file), `benign_own_worktree.json`, `benign_quoted.json` (`git commit -m "revert the git checkout"`), `benign_stash_list.json`, `benign_solo.json`, `benign_expired.json` (the other lane holder's mtime is older than TTL), `violation_release.json` (`octo ps --release <A>` issued from B through Bash, must deny).
Files (modify): hooks.json (two entries, no order claim); rules.yaml (`ARCHITECTURE.kernel-isolation` GATE fail-closed gateable, two Gate mechanisms, each with EXIT_CODE `scripts/<gate> --selftest registry/fixtures/ARCHITECTURE.kernel-isolation`); registry/reflex-triage.yaml:155-159 (mechanism `scripts/g__pretool-bash__tree-owner.py`, `decided` updated); CLAUDE.md Session/Instance Isolation paragraph (one sentence naming both gates); skills/session-isolation/SKILL.md:35-47; docs/architecture/hook-orchestration.md:171 (hooks on one event run in parallel; the BT priority stays specified, not implemented).
Memory sequencing: the private memory repo gets one commit BEFORE the brain PR merges, appending to `lesson_session_isolation_worktree.md` the exact form `scripts/g__pretool-bash__tree-owner.py` (the `_MEM_SCRIPT` regex at brain_doctor.py:1449 matches `scripts/g__*__*.py` bare), so `incident-fixture-coverage` finds the pair on the first doctor run after merge.
Selftest: bespoke; sandbox HOME with a ptable of live processes (fresh timestamps, `_is_live` rejects skew, session-isolation-hook.py:108-121), seeded journals with fresh mtimes attributing `a.py` to A (and one stale journal for `benign_expired`), every fixture through the real `main()`, judged with `gate_selftest.emits_block` (gate_selftest.py:46-70, a plain module function).
Doctor check `kernel-isolation-gate`: both selftests pass; both scripts are in hooks.json at their matchers (no order).
QA proof: reproduce the weekend: two subagents on one worktree, the second told to `git checkout -- <first's files>`; observe the deny naming the first agent's pid and age, the `REFUSED` line in the second agent's `octo replay`, the first agent's files intact; then `rm -rf` on that directory, denied; then both with `isolation: worktree`, no deny; then `octo ps --release` on the first agent and the checkout passes.
Size: 2 new scripts + 11 fixtures, 6 modified, about 700 lines.

### Phase 3: PROCESS quotas and exit status

Goal: a process carries a quota it cannot exceed (tool calls, wall minutes, USD when the journal has tokens), enforced in the same hot-path script. Residual as shipped: the USD axis is NOT implemented, because no runtime writes tokens into the journal yet, so a cap in dollars would be enforced against a number that is always zero; tool calls and wall minutes are the two axes that exist, and the USD one waits for Phase 4's token capture.

Files (create): `registry/kernel.yaml` (tracked policy; public defaults unlimited: `subagent: {max_tool_calls: 0, max_minutes: 0}`; `qa_multiplier: 3` for agent types matching the receipt_ledger.py:461 QA regex, a multiplier not an exemption, since the main loop picks `subagent_type` and a worker named "Reality Checker" must not bypass a cap); caps in gitignored `company/config/kernel.json`, the memory-model.md:78-83 "slot not occupant" pattern; `registry/fixtures/FLOW.kernel-quota/{violation.json, violation_minutes.json, benign.json, benign_qa.json (QA type under 3x cap), violation_qa.json (QA type over 3x cap), home/}` (seed journal with 401 tool lines and a `start_ts`, seed `company/config/kernel.json` cap 400, the `FLOW.budget-halt/home` precedent).
Files (modify): `scripts/g__pretool__kernel.py` (reads `seq` and `start_ts` from the tail line it already holds under the lock; denies with the breached quota named, journals `quota`; `exit: quota` written by the exit hook, not on the hot path); `r__subagent-stop__proc-exit.py` (exit precedence quota > error > ok); `octo ps` shows USED/CAP and EXIT; rules.yaml (`FLOW.kernel-quota` GATE fail-closed, mechanism `g__pretool__kernel.py`, EXIT_CODE `--selftest registry/fixtures/FLOW.kernel-quota`); README FinOps line; CLAUDE.md anchor "Kernel: quotas".
Selftest: `gate_selftest.run_gate_selftest` with the seed.
Doctor check `kernel-quota-live`: selftest passes; `registry/kernel.yaml` validates; `company/config/kernel.json` validates when present.
QA proof: set `max_tool_calls: 5` in a throwaway `company/config/kernel.json`, launch a 10-step subagent, observe the deny on call 5 and `octo ps` showing `EXIT quota` (5, not 6: a registered child's `start` line is a journal line, and the cap counts journal lines); launch a QA-typed subagent with the same cap and a 20-step task, denied on call 15; restore config.
Size: 1 new file + fixtures, 6 modified, about 250 lines.

### Phase 4: JOURNAL closure: every refusal, every receipt, replay proven on real runs

Files (create): `scripts/r__permission-denied__journal.py` (PermissionDenied: journals auto-mode denials, the only kind the runtime documents; the PR's QA proof records whether the event also fires for `permission-rule` and `user-rejected` denials, and only observed kinds are claimed); `scripts/tests/test_octo_replay.py`.
Files (modify): every fail-closed gate that denies or blocks gets a 3-line `kernel_proc.journal_deny(rule_id, reason)` inside its deny helper, wrapped fail-open so a journal error never changes a verdict (dimension-awareness-hook.py:383-388, qa-merge-gate.py, g__pretool-mcp__outward-send.py, g__pretool-bash__git-discipline.py, secrets-grep-guard.py, budget-check.py, the seven `g__stop__*` gates); receipt_ledger.py:235-248 mirrors each receipt as a minimal `receipt` line {kind, ts, tool_use_id, sha256(record)}, nothing else copied; `octo replay` prints a run summary (tools by name, refused by rule, receipts by kind, children, exit); `octo top` adds denies by rule; scripts/brain-digest.py gains a kernel section; schemas/kernel-journal.schema.json; hooks.json (PermissionDenied entry); rules.yaml (`FLOW.kernel-journal` gains the reflex mechanism and a second proof EXIT_CODE `scripts/octo.py replay --fixture registry/fixtures/ARCHITECTURE.kernel-process/replay --verify`); the golden fixture extended with a receipt and a child.
Doctor check `kernel-replay`: golden replay matches; the 5 newest real journals replay and verify; every `deny` line in the last 7 days names a registered rule id (an unregistered deny is an orphan mechanism, RULE #1).
QA proof: trigger one real hook deny (broad `git add -A` on a shared tree), one auto-mode denial, one seek receipt; read all three back from `octo replay <session>` with rule id, source and `tool_use_id`; record which `toolDenialKind` values reached the reflex.
Size: 2 new files, 16 modified, about 400 lines.

### Phase 5: PACKAGE: skills installable like services, arms installable and validated

Files (create): `scripts/octo_pkg.py` (`octo pkg install|verify|uninstall|list|lock|sync`; install reuses the zip and sparse-checkout paths of install-skill-from-github.py:80,:128 with destination `~/.claude/skills/vendor/<name>` (gitignored, the `skills/learned/` precedent at .gitignore:67) or `agents/vendor/<name>`, then creates `skills/<name>` as a symlink to it so the runtime's documented discovery path (`~/.claude/skills/<name>/SKILL.md`, symlinks followed) loads it, and appends `skills/<name>` to `.git/info/exclude` (per machine, untracked); `uninstall` removes both; before copy: `skill.json` validates (validate-skill-manifest.py:48-57), the recomputed `tree_sha256` over every file except `skill.json` and `skill.json.sig` must equal the embedded one, then `ssh-keygen -Y verify -f <allowed-signers> -I <principal> -n octorato-pkg -s skill.json.sig` must pass against `registry/pkg-signers.pub` (principal `octorato-release`) or the gitignored `company/config/pkg-signers` (adopter-added principals; the file whose principal matches is passed with `-f`); the signature covers the manifest, hence the tree; `sync` installs from the lock and verifies, an empty lock is a no-op, a fetch failure prints WARN and continues; `install --kind arm <git-url> --dest <path>` (or `company/config/arms-root`) clones there, validates `arm.json`, appends the path to `company/config/arms-paths.json`, runs `sync-ai-docs` for it; arms are validated, not signed); `packages.lock.json` (tracked at brain root, entries {name, kind, version, tree_sha256, signer, source, installed_at}); `registry/pkg-signers.pub` (one line, `octorato-release ssh-ed25519 AAAA...`, role principal, no email, no hostname); `templates/skill/skill.json.template`, `templates/arm/arm.json.template`; `scripts/gen_skill_manifests.py` (one-shot: `skill.json` from SKILL.md front matter, version 1.0.0, license from LICENSE.txt or the repo default); `registry/fixtures/META.kernel-package/` (sample package tree with `tree_sha256`, a tampered copy, an unsigned copy; NO private key, push-policy.txt:50 blocks key blocks; the selftest generates a throwaway ed25519 key with `ssh-keygen -t ed25519 -N ""` in the sandbox and signs the sample at run time); `scripts/tests/test_octo_pkg.py`.
Files (modify): schemas/skill-manifest.schema.json (add `kind: skill|arm`, `tree_sha256`, `signature`; `additionalProperties` stays false); skills/skill-installer/SKILL.md (Octorato-first; unsigned third-party skills stay on the `--dest` Codex path); scripts/ai_sync.py:285-329 `pull` calls `python3 scripts/octo_pkg.py sync` directly (never the thunk, absent on a fresh clone until install-runners.py runs) before the doctor step at :329; scripts/generate_neural_map.py:531 excludes `skills/vendor/` and `agents/vendor/` from the tracked map (vendor packages are indexed only in the gitignored private connectome layer `company/connectome/`, else a private package name lands in the public graph); scripts/capability_manifest.py:75-80 skips symlinked skill dirs so `docs/CAPABILITIES.md` stays machine-independent; skills/workspace-skill-discovery/SKILL.md lists vendor packages at session start; brain_doctor `CHECKS` (`packages-verified`: PASS `0/0` on an empty lock, WARN with the unlock `octo pkg sync` when an entry is absent on disk, FAIL only on a present entry whose tree hash, signature or symlink does not match; `--fix` runs `sync`; a probe that `ssh-keygen -Y` is supported, FAIL with the unlock on old OpenSSH); .githooks/pre-push runs `octo pkg verify --all` in the WIRE-GATE stanza (`verify --all` exits 1 only on the FAIL tier: a present entry with a tree hash, signature or symlink mismatch, or a lock entry whose signer is in no allowed-signers file; an entry absent on disk prints WARN with `octo pkg sync` and exits 0, so a second machine that pulled the lock offline can still push an unrelated change); CLAUDE.md anchor "Kernel: packages"; docs/wiki/Getting-Started.md; rules.yaml (`META.kernel-package` GATE, `firing_mode: [cli, git-hook]`, mechanism Gate `octo_pkg.py` at PrePush (rules.yaml:18 precedent), proofs EXIT_CODE `scripts/octo_pkg.py --selftest registry/fixtures/META.kernel-package` and `grep -q "octo pkg verify" .githooks/pre-push`).
Selftest: sandbox HOME, throwaway key: install the signed sample (exit 0, symlink present, exclude entry present), refuse the unsigned one (exit 1, nothing copied), refuse the tampered tree before the signature check, `verify --all` FAILs after one installed byte is edited, `uninstall` removes both paths and updates the lock, `sync` on an empty vendor dir restores the lock's packages, `sync` with an empty lock is a no-op; `verify --all` exits 0 on an absent-on-disk entry (WARN) and 1 on a mismatched present entry (FAIL).
QA proof: install the sample from a public test repo path, confirm the skill loads in a fresh session, edit one file, see `octo pkg verify` and the doctor red, uninstall, see the lock shrink and the doctor green; on a second checkout run `ai-pull` and see the package reappear from the lock; push a branch with a tampered lock and watch pre-push refuse; `octo pkg install --kind arm` a throwaway repo into a temp `--dest` and see it in `arms-paths.json`.
Size: 9 new files, 11 modified, about 950 lines, plus the mechanical manifest PR (233 generated `skill.json`, reviewed by diff, mechanical tier).

## 4. v8.0.0 release criterion (printed by brain_doctor at this tag)

- `Kernel: 4/4 primitives live` with `kernel-process-live`, `kernel-isolation-gate`, `kernel-quota-live`, `kernel-replay`, `packages-verified` all PASS.
- Isolation gate fixture-proven: 6 block + 6 allow, `violation_sibling.json` among them.
- Replay: the 5 newest real journals replay and verify; 0 unregistered deny rule ids in 7 days; `open` calls in 7 days printed; `octo bench` median printed (WARN above 100 ms).
- Packages: n/n verified (skills signed; arms validated, not signed), `skill.json` on every skill directory, lock tracked, pre-push verifies; unsigned third-party skills stay outside the lock on the Codex `--dest` path.
- The full check tally, cut with the `Octorato-Major:` trailer.

Not a claim that an agent cannot misbehave, and not a claim of a runtime: processes can be starved, not killed. A claim that every run has a pid and a journal, that a non-owner cannot touch another process's files while the owner is live, that a quota breach stops the process at the tool boundary, and that every installed skill is what was signed.

## 5. Risks and the decisions only the operator can make

Risks: (1) A single Python hook on `PreToolUse *` costs about 80 to 100 ms per call on the reference box (interpreter 33 ms, stdlib imports 66 ms); the bench is reported, never gating, and journal + quota stay ONE invocation. (2) Two process tables (`sessions.json`, ptable) for one release; the lane fix lives in the ptable only, and the merge is v8.1 debt. (3) Cursor cannot project SubagentStart, PermissionDenied or Agent-scoped events (multi-runtime.md:64); the doctor names it. (4) `ssh-keygen -Y` absent on old Windows OpenSSH; verify degrades to FAIL with the unlock, never to skip. (5) A runtime without `SubagentStart` loses the child's start line until its first tool call; the fallback is recorded in the doctor output. (6) A crashed child holds its lanes for up to 15 minutes; `octo ps --release` is the operator's early out.

Decisions:
1. New `KERNEL` category in rules.schema.json:31, or reuse ARCHITECTURE/FLOW/META? Default: reuse; a category is a schema PR with zero enforcement gain.
2. Signing: `ssh-keygen -Y` with allowed-signers files (verified offline here, OpenSSH 9.6p1) or minisign? Default: ssh-keygen; the `octorato-release` private key stays with the operator, only its public line ships; adopters add principals in `company/config/pkg-signers`.
3. Manifests for the 233 existing skills: generate `skill.json` for all of them in one mechanical PR before the cut (default), or grandfather with a doctor WARN? Default: generate; a standing WARN at the cut contradicts the release claim. Grandfather only if the generator hits a measured blocker (a skill with no parseable front matter gets a hand-written manifest).

## 6. Out of scope for v8 (stays M4/M5 future)

Kill, suspend, wait or signal a process; scheduler; IPC or message bus; checkpoint and restore (M4). A hosted package index, dependency resolution beyond the validated `dependencies` map, signed arms (M5). The LinUCB tier router (hook-orchestration.md:189) and an integer hook priority field (:184). Tamper-proof journals (the chain gives tamper evidence, the file sits under `$HOME`, the same residual v7 states). Merging `sessions.json` into the ptable. Journal lines carry hashes of tool input, not content: a run is verifiable and its verdicts reconstructible; full content lives in the harness transcript. User rejections in default mode have no documented hook event and stay a residual unless Phase 4's QA proof observes one.

## 7. Panel findings not adopted

None. Every second-pass item from both panels is folded in: vendor install with symlink and `.git/info/exclude` (the env-var alternative was not chosen because it needs the operator's shell env); lane-claim write on the hot path stated and benched; child liveness by own mtime plus parent, main liveness by own or any child's mtime, `octo ps --release`; PermissionDenied scoped to auto-mode with observed kinds only; `start_ts` on every line; prefix semantics for `rm -rf`; second allowed-signers file; arms `--dest`, validated not signed; `REFUSED` pairing by `tool_use_id`; fail-closed journal with the three conditions; bench never a gate; `octo_pkg.py sync` called directly with the PASS/WARN/FAIL ladder; vendor excluded from the tracked neural map and capability manifest; QA 3x multiplier; reflex scripts without fixture pairs; 1a split into 1a and 1a-2. The skill count is 233 (`find skills -maxdepth 2 -name SKILL.md`), the earlier 234 counted one directory without a `SKILL.md`.
