---
name: sample-unsigned
description: Sample package used by the PACKAGE selftest. Does nothing; exists to be hashed, signed, installed and verified.
---

# Sample Package

A minimal, inert skill body. The selftest never loads it; it only installs it,
hashes it, verifies its signature, syncs it from the lock and removes it.
