# Octorato Rule Registry — single source of truth for RULE #1 (Wired or Corrupt).
# Every CLAUDE.md rule maps here to its backing mechanism(s) + proof.
# Validated by brain_doctor (D1 schema, D2 anchors, D3 per-rule wiring).
# Phase 0 seed: the already-wired mechanisms, existing script names (renaming is Phase 3).
# Schema: registry/rules.schema.json   Design: docs/architecture/wired-or-corrupt.md
version: 1
rules:
  # ---- META: Rule #1 is self-wired; its mechanism IS brain_doctor + the pre-push gate ----
  - id: META.rule-1-wired-or-corrupt
    title: "RULE #1 — every rule must be wired; unwired = corrupt brain"
    category: META
    source: { file: CLAUDE.md, anchor: "RULE #1" }
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode: [runner, git-hook]
    mechanism:
      - { kind: Gate, canonical_name: brain_doctor.py, firing_event: PrePush, firing_matcher: null }
    proof:
      - { method: FILE_EXISTS, locator: "scripts/brain_doctor.py", expect: present }
      - { method: ANCHOR_PRESENT, locator: "RULE #1", expect: present }
    liveness_required: PRESENT

  - id: META.pre-push-gate
    title: "brain_doctor is invoked from the tracked pre-push hook"
    category: META
    source: { file: CLAUDE.md, anchor: "RULE #1" }
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode: [git-hook]
    mechanism:
      - { kind: Gate, canonical_name: .githooks/pre-push, firing_event: PrePush, firing_matcher: null }
    proof:
      - { method: FILE_EXISTS, locator: ".githooks/pre-push", expect: present }
      - { method: EXIT_CODE, locator: "grep -q OCTORATO-WIRE-GATE .githooks/pre-push", expect: 0 }
    liveness_required: PRESENT

  # ---- COMMS ----
  - id: COMMS.human-cadence
    title: "Human Cadence — 10 anti-AI-tell rules"
    category: COMMS
    source: { file: CLAUDE.md, anchor: "Human Cadence" }
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode: [hook, cli]
    mechanism:
      - { kind: Gate, canonical_name: cadence-stop-hook.py, firing_event: Stop, firing_matcher: "*" }
      - { kind: Detector, canonical_name: cadence-lint.py, firing_event: PostToolUse, firing_matcher: "Write|Edit" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "Stop|*|cadence-stop-hook.py", expect: present }
      - { method: IN_HOOKS_JSON, locator: "PostToolUse|Write|Edit|cadence-lint.py", expect: present }
    liveness_required: FIRES

  # ---- CODE ----
  - id: CODE.adversarial-verify-operator
    title: "Adversarially verify the operator; claims need proof"
    category: CODE
    source: { file: CLAUDE.md, anchor: "Adversarially verify the operator" }
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode: [hook]
    mechanism:
      - { kind: Gate, canonical_name: claim-verify-stop.py, firing_event: Stop, firing_matcher: "*" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "Stop|*|claim-verify-stop.py", expect: present }
    liveness_required: FIRES

  - id: CODE.cite-sources
    title: "Cite sources; every answer ends with its provenance"
    category: CODE
    source: { file: CLAUDE.md, anchor: "Cite sources" }
    strength: DETECTOR
    gateable: true
    enforcement: fail-open
    waiver:
      reason: "Detector (source-attribution) is warn-only; promoting to a hard Stop block is deferred pending false-positive tuning so legit turns are not blocked. Close candidate."
      expires: "2026-09-30"
      approved_by: "operator directive 2026-06-29 (cierra o waiva las 7)"
    firing_mode: [hook]
    mechanism:
      - { kind: Detector, canonical_name: source-attribution-check.py, firing_event: Stop, firing_matcher: "*" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "Stop|*|source-attribution-check.py", expect: present }
    liveness_required: FIRES

  - id: CODE.qa-merge-gate
    title: "Merges are fail-closed; agent cannot self-approve"
    category: CODE
    source: { file: CLAUDE.md, anchor: "qa-merge-gate" }
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode: [hook]
    mechanism:
      - { kind: Gate, canonical_name: qa-merge-gate.py, firing_event: PreToolUse, firing_matcher: "Bash" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "PreToolUse|Bash|qa-merge-gate.py", expect: present }
    liveness_required: FIRES

  - id: CODE.dry-run-first
    title: "Dry-run first; ask before shipping generated configs"
    category: CODE
    source: { file: CLAUDE.md, anchor: "Dry-run first" }
    strength: DETECTOR
    gateable: true
    enforcement: fail-open   # config-ship-verify.py is PreToolUse:Bash but warn-only; a real deny is the close path
    waiver:
      reason: "config-ship-verify is warn-only; a deterministic deny of 'ship generated config' is not yet specifiable without false positives. Deferred. Close candidate."
      expires: "2026-09-30"
      approved_by: "operator directive 2026-06-29 (cierra o waiva las 7)"
    firing_mode: [hook]
    mechanism:
      - { kind: Detector, canonical_name: config-ship-verify.py, firing_event: PreToolUse, firing_matcher: "Bash" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "PreToolUse|Bash|config-ship-verify.py", expect: present }
    liveness_required: FIRES

  # ---- SECURITY ----
  - id: SECURITY.never-read-secrets-raw
    title: "Never echo/read secret-bearing files raw"
    category: SECURITY
    source: { file: CLAUDE.md, anchor: "Never commit secrets" }
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode: [hook]
    mechanism:
      - { kind: Gate, canonical_name: secrets-grep-guard.py, firing_event: PreToolUse, firing_matcher: "Bash" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "PreToolUse|Bash|secrets-grep-guard.py", expect: present }
    liveness_required: FIRES

  # ---- GENERIC ----
  - id: GENERIC.brain-stays-generic
    title: "Brain stays generic; no arm/client identifiers leak"
    category: GENERIC
    source: { file: CLAUDE.md, anchor: "The Brain Stays Generic" }
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode: [git-hook, runner]
    mechanism:
      - { kind: Gate, canonical_name: check-generic.py, firing_event: PrePush, firing_matcher: null }
    proof:
      - { method: FILE_EXISTS, locator: "scripts/check-generic.py", expect: present }
    liveness_required: PRESENT

  # ---- ARCHITECTURE ----
  - id: ARCHITECTURE.session-isolation
    title: "Session/instance isolation; own worktree per dimension"
    category: ARCHITECTURE
    source: { file: CLAUDE.md, anchor: "Session/Instance Isolation" }
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode: [hook]
    mechanism:
      - { kind: Gate, canonical_name: session-isolation-hook.py, firing_event: SessionStart, firing_matcher: null }
      - { kind: Gate, canonical_name: dimension-awareness-hook.py, firing_event: PreToolUse, firing_matcher: "Bash" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "SessionStart|*|session-isolation-hook.py", expect: present }
      - { method: IN_HOOKS_JSON, locator: "PreToolUse|Bash|dimension-awareness-hook.py", expect: present }
    liveness_required: PRESENT

  # ---- FLOW ----
  - id: FLOW.4d-paradigm
    title: "4D paradigm fires each turn (Describe/Delegate/Diligent/Disclose)"
    category: FLOW
    source: { file: CLAUDE.md, anchor: "The 4D Paradigm" }
    strength: REFLEX
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: 4d-reminder.py, firing_event: UserPromptSubmit, firing_matcher: null }
    proof:
      - { method: IN_HOOKS_JSON, locator: "UserPromptSubmit|*|4d-reminder.py", expect: present }
    liveness_required: FIRES

  - id: FLOW.delegate-gate
    title: "2D Delegate: connect, don't SELF (heartbeat + delegate reflex)"
    category: FLOW
    source: { file: CLAUDE.md, anchor: "2D Delegate Gate" }
    strength: REFLEX
    gateable: true
    enforcement: fail-open   # NOT closeable today: PreToolUse:Bash fires inside sub-agents too and carries no main-loop-vs-sub-agent discriminator, so a deny self-defeats (denies the sub-agent it delegates to) AND breaks 3D Diligent. Stays a nudge; listed here permanently as model-discretion until the platform exposes a caller discriminator. (QA-proven 2026-06-29.)
    waiver:
      reason: "Not closeable: PreToolUse carries no main-loop vs sub-agent discriminator, so a deny self-defeats and breaks 3D Diligent (QA-proven 2026-06-29). Revisit if the platform exposes a caller field."
      expires: "2026-09-30"
      approved_by: "operator directive 2026-06-29 (cierra o waiva las 7)"
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: connectome-heartbeat.py, firing_event: UserPromptSubmit, firing_matcher: null }
      - { kind: Reflex, canonical_name: delegate-gate.py, firing_event: PreToolUse, firing_matcher: "Bash" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "UserPromptSubmit|*|connectome-heartbeat.py", expect: present }
      - { method: IN_HOOKS_JSON, locator: "PreToolUse|Bash|delegate-gate.py", expect: present }
    liveness_required: FIRES

  - id: FLOW.graph-before-grep
    title: "Graph before grep; seek the lineage, do not scan"
    category: FLOW
    source: { file: CLAUDE.md, anchor: "Graph before grep" }
    strength: REFLEX
    gateable: true
    enforcement: fail-open
    waiver:
      reason: "grafo-gate injects and grafo-ledger-check is warn-only; a hard Stop block on missing-seek is deferred pending false-positive tuning (legit external greps). Close candidate."
      expires: "2026-09-30"
      approved_by: "operator directive 2026-06-29 (cierra o waiva las 7)"
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: grafo-gate.py, firing_event: PreToolUse, firing_matcher: "Bash" }
      - { kind: Reflex, canonical_name: grafo-turn-reset.py, firing_event: UserPromptSubmit, firing_matcher: null }
      - { kind: Detector, canonical_name: grafo-ledger-check.py, firing_event: Stop, firing_matcher: "*" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "PreToolUse|Bash|grafo-gate.py", expect: present }
      - { method: IN_HOOKS_JSON, locator: "Stop|*|grafo-ledger-check.py", expect: present }
    liveness_required: FIRES

  - id: FLOW.impact-radius
    title: "Impact Radius on every concept change"
    category: FLOW
    source: { file: CLAUDE.md, anchor: "Impact Radius on every concept change" }
    strength: REFLEX
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: impact-radius-hook.py, firing_event: PostToolUse, firing_matcher: "Write|Edit" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "PostToolUse|Write|Edit|impact-radius-hook.py", expect: present }
    liveness_required: FIRES

  - id: FLOW.web-agent-browser
    title: "Web inspection uses agent-browser (eyes), not curl"
    category: FLOW
    source: { file: CLAUDE.md, anchor: "agent-browser" }
    strength: REFLEX
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: eye-check.py, firing_event: UserPromptSubmit, firing_matcher: null }
    proof:
      - { method: IN_HOOKS_JSON, locator: "UserPromptSubmit|*|eye-check.py", expect: present }
    liveness_required: FIRES
  - id: IDENTITY.self-awareness
    title: Self-awareness — Octorato identity anchor
    category: IDENTITY
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "Self-Awareness — You Are Running on Octorato"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "Self-Awareness — You Are Running on Octorato"
        expect: present
    liveness_required: PRESENT

  - id: IDENTITY.generic-stance
    title: Generic stance — Octorato identity non-negotiable
    category: IDENTITY
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "Octorato's Stance (Generic Identity"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "Octorato's Stance (Generic Identity"
        expect: present
    liveness_required: PRESENT

  - id: ARCHITECTURE.octopus-architecture
    title: Octopus architecture — structural model anchor
    category: ARCHITECTURE
    strength: DETECTOR
    gateable: false   # repo-state/anchor-presence (enforced at push by anchor + check-generic), not a model-action discretion gate
    enforcement: n/a
    firing_mode:
      - git-hook
    source:
      file: CLAUDE.md
      anchor: "## The Octopus Architecture"
    mechanism:
      - kind: Detector
        canonical_name: check-generic.py
        firing_event: PrePush
        firing_matcher: null
    proof:
      - method: FILE_EXISTS
        locator: scripts/check-generic.py
        expect: present
      - method: ANCHOR_PRESENT
        locator: "## The Octopus Architecture"
        expect: present
    liveness_required: PRESENT

  - id: ARCHITECTURE.core-principles
    title: Core principles — arm isolation gates
    category: ARCHITECTURE
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode:
      - hook
    source:
      file: CLAUDE.md
      anchor: "### Core Principles"
    mechanism:
      - kind: Gate
        canonical_name: session-isolation-hook.py
        firing_event: SessionStart
        firing_matcher: null
      - kind: Gate
        canonical_name: dimension-awareness-hook.py
        firing_event: PreToolUse
        firing_matcher: Bash
    proof:
      - method: IN_HOOKS_JSON
        locator: "SessionStart|*|session-isolation-hook.py"
        expect: present
      - method: IN_HOOKS_JSON
        locator: "PreToolUse|Bash|dimension-awareness-hook.py"
        expect: present
      - method: ANCHOR_PRESENT
        locator: "### Core Principles"
        expect: present
    liveness_required: FIRES

  - id: ARCHITECTURE.information-flow
    title: Information flow rules — cross-arm leak detector
    category: ARCHITECTURE
    strength: DETECTOR
    gateable: false   # the cross-arm leak block lives in GENERIC.brain-stays-generic (already gateable+fail-closed); this row is the policy/anchor
    enforcement: n/a
    firing_mode:
      - git-hook
    source:
      file: CLAUDE.md
      anchor: "### Information Flow Rules"
    mechanism:
      - kind: Detector
        canonical_name: check-generic.py
        firing_event: PrePush
        firing_matcher: null
    proof:
      - method: FILE_EXISTS
        locator: scripts/check-generic.py
        expect: present
      - method: ANCHOR_PRESENT
        locator: "### Information Flow Rules"
        expect: present
    liveness_required: PRESENT

  - id: ARCHITECTURE.layers
    title: Layer separation — memory tier enforcement
    category: ARCHITECTURE
    strength: DETECTOR
    firing_mode:
      - runner
    source:
      file: CLAUDE.md
      anchor: "### Layers"
    mechanism:
      - kind: Detector
        canonical_name: memory_sync.py
        firing_event: null
        firing_matcher: null
    proof:
      - method: FILE_EXISTS
        locator: scripts/memory_sync.py
        expect: present
      - method: ANCHOR_PRESENT
        locator: "### Layers"
        expect: present
    liveness_required: PRESENT

  - id: CODE.universal-discipline
    title: Universal code discipline — baseline rules
    category: CODE
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "## Universal Code Discipline"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "## Universal Code Discipline"
        expect: present
    liveness_required: PRESENT

  - id: CODE.do-it-right
    title: Do it right not fast — root cause over palliative
    category: CODE
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "ULTRA RULE — Do it right, not fast"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "ULTRA RULE — Do it right, not fast"
        expect: present
    liveness_required: PRESENT

  - id: SECURITY.baseline
    title: Security baseline — secrets gate at Bash and push
    category: SECURITY
    strength: GATE
    gateable: true
    enforcement: fail-closed
    firing_mode:
      - hook
      - git-hook
    source:
      file: CLAUDE.md
      anchor: "## Security (Non-Negotiable)"
    mechanism:
      - kind: Gate
        canonical_name: secrets-grep-guard.py
        firing_event: PreToolUse
        firing_matcher: Bash
      - kind: Gate
        canonical_name: check-generic.py
        firing_event: PrePush
        firing_matcher: null
    proof:
      - method: IN_HOOKS_JSON
        locator: "PreToolUse|Bash|secrets-grep-guard.py"
        expect: present
      - method: FILE_EXISTS
        locator: scripts/check-generic.py
        expect: present
      - method: ANCHOR_PRESENT
        locator: "## Security (Non-Negotiable)"
        expect: present
    liveness_required: FIRES

  - id: CODE.prompt-defense
    title: PromptDefense baseline — anti-injection rules
    category: CODE
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "## PromptDefense Baseline"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "## PromptDefense Baseline"
        expect: present
    liveness_required: PRESENT

  - id: COMMS.communication-baseline
    title: Communication baseline — cadence stop detector
    category: COMMS
    strength: DETECTOR
    gateable: true
    enforcement: fail-closed
    firing_mode:
      - hook
    source:
      file: CLAUDE.md
      anchor: "## Communication"
    mechanism:
      - kind: Detector
        canonical_name: cadence-stop-hook.py
        firing_event: Stop
        firing_matcher: "*"
    proof:
      - method: IN_HOOKS_JSON
        locator: "Stop|*|cadence-stop-hook.py"
        expect: present
      - method: ANCHOR_PRESENT
        locator: "## Communication"
        expect: present
    liveness_required: FIRES

  - id: COMMS.machine-register
    title: Machine register — no human-social filler
    category: COMMS
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "ULTRA RULE — Machine register"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "ULTRA RULE — Machine register"
        expect: present
    liveness_required: PRESENT

  - id: GIT.version-control
    title: Git and version control — atomic commit discipline
    category: GIT
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "## Git & Version Control"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "## Git & Version Control"
        expect: present
    liveness_required: PRESENT

  - id: CODE.file-organization
    title: File organization — config-first no summary markdown
    category: CODE
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "## File Organization"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "## File Organization"
        expect: present
    liveness_required: PRESENT

  - id: CODE.when-unsure
    title: When unsure — search before assuming
    category: CODE
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "## When Unsure"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "## When Unsure"
        expect: present
    liveness_required: PRESENT

  - id: FLOW.prune-dead-cells
    title: Prune dead cells — lineage-doctor detects orphans at push
    category: FLOW
    strength: DETECTOR
    gateable: false   # advisory push detector (lineage-doctor warn-only); rule is "suggest, never auto-delete" by design
    enforcement: n/a
    firing_mode:
      - git-hook
    source:
      file: CLAUDE.md
      anchor: "ULTRA RULE — Prune dead cells"
    mechanism:
      - kind: Detector
        canonical_name: lineage-doctor.py
        firing_event: PrePush
        firing_matcher: null
    proof:
      - method: FILE_EXISTS
        locator: scripts/lineage-doctor.py
        expect: present
      - method: ANCHOR_PRESENT
        locator: "ULTRA RULE — Prune dead cells"
        expect: present
    liveness_required: PRESENT

  - id: FLOW.suggest-unlock
    title: Suggest the unlock — disclose capability gaps
    category: FLOW
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "ULTRA RULE — Suggest the unlock"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "ULTRA RULE — Suggest the unlock"
        expect: present
    liveness_required: PRESENT

  - id: FLOW.4d-gate-prewrite
    title: 4D pre-write manifest gate
    category: FLOW
    strength: REFLEX
    firing_mode:
      - hook
      - cli
    source:
      file: CLAUDE.md
      anchor: "### 4D Gate (Pre-Write Manifest)"
    mechanism:
      - kind: Reflex
        canonical_name: 4d-reminder.py
        firing_event: UserPromptSubmit
        firing_matcher: null
      - kind: Reflex
        canonical_name: gate-check
        firing_event: null
        firing_matcher: null
    proof:
      - method: IN_HOOKS_JSON
        locator: "UserPromptSubmit|*|4d-reminder.py"
        expect: present
      - method: FILE_EXISTS
        locator: scripts/gate-check
        expect: present
      - method: ANCHOR_PRESENT
        locator: "### 4D Gate (Pre-Write Manifest)"
        expect: present
    liveness_required: FIRES

  - id: FLOW.3d-diligent-gate
    title: 3D diligent gate — post-write validation
    category: FLOW
    strength: REFLEX
    gateable: true
    enforcement: fail-open   # shares source-attribution-check.py (Stop, warn-only); same class as CODE.cite-sources
    waiver:
      reason: "Shares the warn-only source-attribution Stop detector; the 'did you validate' part is judgment. Hard block deferred. Close candidate."
      expires: "2026-09-30"
      approved_by: "operator directive 2026-06-29 (cierra o waiva las 7)"
    firing_mode:
      - hook
    source:
      file: CLAUDE.md
      anchor: "### 3D Diligent Gate"
    mechanism:
      - kind: Reflex
        canonical_name: trace-hook.py
        firing_event: PostToolUse
        firing_matcher: "Write|Edit"
      - kind: Detector
        canonical_name: source-attribution-check.py
        firing_event: Stop
        firing_matcher: "*"
    proof:
      - method: IN_HOOKS_JSON
        locator: "PostToolUse|Write|Edit|trace-hook.py"
        expect: present
      - method: IN_HOOKS_JSON
        locator: "Stop|*|source-attribution-check.py"
        expect: present
      - method: ANCHOR_PRESENT
        locator: "### 3D Diligent Gate"
        expect: present
    liveness_required: FIRES

  - id: FLOW.4d-spec-enhancement
    title: 4D+S spec-driven enhancement — complexity classifier
    category: FLOW
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "### 4D+S — Spec-Driven Enhancement"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "### 4D+S — Spec-Driven Enhancement"
        expect: present
    liveness_required: PRESENT

  - id: FLOW.enforcement-scripts
    title: Enforcement scripts — mandatory Q1/Q3 reflexes
    category: FLOW
    strength: REFLEX
    gateable: true
    enforcement: fail-open   # shares delegate-gate.py nudge mechanism (see FLOW.delegate-gate)
    waiver:
      reason: "Shares delegate-gate.py, which is not closeable (no caller discriminator). Tied to FLOW.delegate-gate."
      expires: "2026-09-30"
      approved_by: "operator directive 2026-06-29 (cierra o waiva las 7)"
    firing_mode:
      - hook
      - cli
    source:
      file: CLAUDE.md
      anchor: "### Enforcement Scripts (Mandatory)"
    mechanism:
      - kind: Reflex
        canonical_name: connectome-heartbeat.py
        firing_event: UserPromptSubmit
        firing_matcher: null
      - kind: Reflex
        canonical_name: delegate-gate.py
        firing_event: PreToolUse
        firing_matcher: Bash
    proof:
      - method: IN_HOOKS_JSON
        locator: "UserPromptSubmit|*|connectome-heartbeat.py"
        expect: present
      - method: IN_HOOKS_JSON
        locator: "PreToolUse|Bash|delegate-gate.py"
        expect: present
      - method: ANCHOR_PRESENT
        locator: "### Enforcement Scripts (Mandatory)"
        expect: present
    liveness_required: FIRES

  - id: CODE.best-tool-first
    title: Best tool first — never settle for workarounds
    category: CODE
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "## Best Tool First"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "## Best Tool First"
        expect: present
    liveness_required: PRESENT

  - id: FLOW.skill-first
    title: Skill-first behavior — domain reflexes via eye-check
    category: FLOW
    strength: REFLEX
    firing_mode:
      - hook
    source:
      file: CLAUDE.md
      anchor: "## Skill-First Behavior"
    mechanism:
      - kind: Reflex
        canonical_name: eye-check.py
        firing_event: UserPromptSubmit
        firing_matcher: null
    proof:
      - method: IN_HOOKS_JSON
        locator: "UserPromptSubmit|*|eye-check.py"
        expect: present
      - method: ANCHOR_PRESENT
        locator: "## Skill-First Behavior"
        expect: present
    liveness_required: FIRES

  - id: META.querymaster
    title: QueryMaster — global database agent anchor
    category: META
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "## QueryMaster — Global Database Agent"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "## QueryMaster — Global Database Agent"
        expect: present
    liveness_required: PRESENT

  - id: ARCHITECTURE.arm-onboarding
    title: Arm onboarding — install-runners bootstrap detector
    category: ARCHITECTURE
    strength: DETECTOR
    firing_mode:
      - runner
    source:
      file: CLAUDE.md
      anchor: "## Arm Onboarding"
    mechanism:
      - kind: Detector
        canonical_name: install-runners.py
        firing_event: null
        firing_matcher: null
    proof:
      - method: FILE_EXISTS
        locator: scripts/install-runners.py
        expect: present
      - method: ANCHOR_PRESENT
        locator: "## Arm Onboarding"
        expect: present
    liveness_required: PRESENT

  - id: META.multi-machine-sync
    title: Multi-machine sync — brain consistency anchor
    category: META
    strength: PRESENCE
    firing_mode:
      - model
    source:
      file: CLAUDE.md
      anchor: "## Multi-Machine Sync"
    mechanism:
      - kind: Presence
        canonical_name: null
        firing_event: null
        firing_matcher: null
    proof:
      - method: ANCHOR_PRESENT
        locator: "## Multi-Machine Sync"
        expect: present
    liveness_required: PRESENT

  - id: META.ai-sync-canonical
    title: ai-sync canonical runner — race-safe brain sync
    category: META
    strength: REFLEX
    firing_mode:
      - runner
    source:
      file: CLAUDE.md
      anchor: "ULTRA RULE: `ai-sync"
    mechanism:
      - kind: Reflex
        canonical_name: ai_sync.py
        firing_event: null
        firing_matcher: null
    proof:
      - method: FILE_EXISTS
        locator: scripts/ai_sync.py
        expect: present
      - method: ANCHOR_PRESENT
        locator: "ULTRA RULE: `ai-sync"
        expect: present
    liveness_required: PRESENT

  # --- D4 (Phase 4b): the 6 previously-orphan hooks, now registered (RULE #1 bidirectional) ---
  - id: MEMORY.arm-recall
    title: "Recall an arm's own stored knowledge before researching or asking"
    category: MEMORY
    source: { file: CLAUDE.md, anchor: "Skill-First Behavior" }
    strength: REFLEX
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: arm-recall-hook.py, firing_event: UserPromptSubmit, firing_matcher: "*" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "UserPromptSubmit|*|arm-recall-hook.py", expect: present }
      - { method: ANCHOR_PRESENT, locator: "Skill-First Behavior", expect: present }
    liveness_required: FIRES
  - id: MEMORY.brain-memory-recall
    title: "Surface the brain's own life-memories relevant to the prompt before asking"
    category: MEMORY
    source: { file: CLAUDE.md, anchor: "When Unsure" }
    strength: REFLEX
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: brain-memory-recall.py, firing_event: UserPromptSubmit, firing_matcher: "*" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "UserPromptSubmit|*|brain-memory-recall.py", expect: present }
      - { method: ANCHOR_PRESENT, locator: "When Unsure", expect: present }
    liveness_required: FIRES
  - id: COMMS.no-pause
    title: "Do not ask to pause; execute, never propose stopping"
    category: COMMS
    source: { file: CLAUDE.md, anchor: "Machine register" }
    strength: DETECTOR
    gateable: true
    enforcement: fail-open
    waiver:
      reason: "no-pause-suggestion Stop is warn-only; a hard block on pause-phrase detection is deferred pending false-positive tuning (legit clarifying questions). Close candidate."
      expires: "2026-09-30"
      approved_by: "operator directive 2026-06-29 (cierra o waiva las 7)"
    firing_mode: [hook]
    mechanism:
      - { kind: Detector, canonical_name: no-pause-suggestion.py, firing_event: Stop, firing_matcher: "*" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "Stop|*|no-pause-suggestion.py", expect: present }
      - { method: ANCHOR_PRESENT, locator: "Machine register", expect: present }
    liveness_required: FIRES
  - id: COMMS.client-doc-lint
    title: "Client deliverables pass the doc lint before they are sent"
    category: COMMS
    source: { file: skills/client-doc-lint/SKILL.md, anchor: "el doc no se manda sin pasar el lint" }
    strength: DETECTOR
    firing_mode: [hook]
    mechanism:
      - { kind: Detector, canonical_name: client-doc-lint-hook.py, firing_event: PostToolUse, firing_matcher: "Bash" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "PostToolUse|Bash|client-doc-lint-hook.py", expect: present }
      - { method: ANCHOR_PRESENT, locator: "el doc no se manda sin pasar el lint", expect: present }
    liveness_required: FIRES
  - id: MEMORY.inbox-sweep
    title: "Full inbox sweep when email or pendientes are requested"
    category: MEMORY
    source: { file: skills/inbox-triage-classifier/SKILL.md, anchor: "Inbox-Triage Classifier" }
    strength: REFLEX
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: inbox-sweep-reflex.py, firing_event: UserPromptSubmit, firing_matcher: "*" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "UserPromptSubmit|*|inbox-sweep-reflex.py", expect: present }
      - { method: ANCHOR_PRESENT, locator: "Inbox-Triage Classifier", expect: present }
    liveness_required: FIRES
  - id: FLOW.canon-heal
    title: "Heal the content canon to unison on every edit"
    category: FLOW
    source: { file: skills/octorato-harmony/SKILL.md, anchor: "Octorato Harmony" }
    strength: REFLEX
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: canon-heal-hook.py, firing_event: PostToolUse, firing_matcher: "Write|Edit" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "PostToolUse|Write|Edit|canon-heal-hook.py", expect: present }
      - { method: ANCHOR_PRESENT, locator: "Octorato Harmony", expect: present }
    liveness_required: FIRES
  - id: ARCHITECTURE.drift-self-heal
    title: "Reconcile settings.json from the tracked hooks.json at session start (drift self-heal)"
    category: ARCHITECTURE
    source: { file: CLAUDE.md, anchor: "Multi-Machine Sync" }
    strength: REFLEX
    firing_mode: [hook]
    mechanism:
      - { kind: Reflex, canonical_name: merge-hooks.py, firing_event: SessionStart, firing_matcher: "*" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "SessionStart|*|merge-hooks.py", expect: present }
      - { method: ANCHOR_PRESENT, locator: "Multi-Machine Sync", expect: present }
    liveness_required: FIRES

  - id: FLOW.budget-halt
    title: "FinOps budget caps halt expensive dispatch (fail-open)"
    category: FLOW
    source: { file: README.md, anchor: "FinOps roadmap" }
    strength: GATE
    gateable: true
    enforcement: fail-open   # FinOps off until budgets.yaml exists; uncapped agent dispatch accepted as a known gap. Waived below.
    waiver:
      reason: "FinOps disabled until budgets.yaml exists; uncapped agent dispatch accepted as a known gap. Enabling per-arm budgets supersedes this waiver."
      expires: "2026-09-30"
      approved_by: "operator directive 2026-06-29 (resuelve budget-halt)"
    firing_mode: [hook]
    mechanism:
      - { kind: Gate, canonical_name: budget-check.py, firing_event: PreToolUse, firing_matcher: "Agent" }
    proof:
      - { method: IN_HOOKS_JSON, locator: "PreToolUse|Agent|budget-check.py", expect: present }
      - { method: ANCHOR_PRESENT, locator: "FinOps roadmap", expect: present }
    liveness_required: FIRES
